# After Coder 機能仕様書

このドキュメントは、`app.py` / `llm_client.py` の実装内容を反映した機能仕様書です。
**実装を変更した際は、このドキュメントも同じタイミングで更新します。**

最終更新: 2026-07-31（コーディング範囲を「しない／100件／200件／全件」の段階選択に変更し、策定〜編集〜追加コーディングを行き来できる統一モデルに再設計）

---

## 1. 全体像

本システムは3つのフェーズで構成される。

```
1. 作業準備 → 2. Codebook Builder（コードブック策定） → 3. After Coder（コーディング・集計）
```

2と3は完全に独立した2段階ではなく、**「コーディング済み件数」という1つの状態を軸にした連続的なモデル**になっている（詳細は4.5節）。コードブックだけを見て編集判断するのは実用的でないという知見から、デフォルトでは少量（100件）をコーディングした状態で結果画面が表示され、そこでコードブックを確認・編集しながら、必要に応じて追加でコーディング範囲を広げていく使い方を想定している。

---

## 2. 認証・セッション管理

- ログインはユーザー名／パスワードの照合で行う。**資格情報はコードに直接書かず`st.secrets`から読む**（2026-08-18変更）。以前はユーザー辞書・管理者パスワードを`app.py`に平文でハードコードしており、公開リポジトリに実際のパスワードがそのままコミットされてしまっていた（実運用中の同僚アカウントの資格情報が漏洩していたことが判明し、緊急対応として修正）。`authenticate()`は`st.secrets.get('admin_username'/'admin_password')`と`st.secrets.get('users', {})`のみを見る。ローカル実行時は`.streamlit/secrets.toml`（`.gitignore`で除外、Gitには一切含まれない）、本番（Streamlit Community Cloud）はアプリ管理画面の「Settings > Secrets」に、以下と同じ形式で設定する。
  ```toml
  admin_username = "admin"
  admin_password = "..."
  [users]
  someuser = "..."
  ```
  secrets自体が未設定（キーが無い、ファイルが無い）の場合は`admin_password`が`None`・`users`が空辞書となり、**誰もログインできないフェイルセーフな挙動**になる（「デフォルトパスワードで通ってしまう」抜け穴が無い）。**旧パスワードは公開履歴に残り続けるため、この変更だけでは不十分——実際のパスワードのローテーション（変更）が別途必要**（コード修正の範囲外、運用対応）。
- セッション状態（`st.session_state`）にログイン状態・分析結果の一覧（内部名`history`、後述）を保持する。ブラウザセッションが切れる、または「ログアウト」を押すと全て消える。永続化（DB保存等）はしていない。
- 分析結果は内部的には直近10件までメモリ上（`st.session_state.history`）に保持されるが、**サイドバーのUI表示は直近3件のみ**（2026-08-19、「🕒 最近使用したプロジェクト」として表示。旧「📜 作業履歴」から改称・表示件数を10→3に削減——プロジェクトファイルの保存・読み込みで代替できるようになったため、長い一覧を常時表示する必要性が下がったという判断）。データ構造・保持件数（10件）自体は変更していない。
- **「🔄 設定をリセット（新しい分析用）」ボタン**（2026-08-05追加、サイドバー最下部・ログアウトボタンの直上）：`st.session_state.xlsx_items`（Excelアップロードの読み込み結果）、`xlsx_text_col`/`xlsx_id_col`/`xlsx_fa_col`/`xlsx_attr_cols`（列マッピングの選択状態）、リスクチェックのチェックボックス（`risk_{key}`）、`texts_count`を消去する。**APIキー・プロジェクト名・分析結果一覧（`history`）は保持される**（ログアウトとの違い）。
  - **経緯**：Streamlitのウィジェット状態（チェックボックス等）はブラウザセッション内で自動的に保持される仕様のため、新しいデータファイルで分析を始めても、前回のリスクチェックのチェック状態や、Excelアップロードで一度読み込んだ`xlsx_items`がそのまま残ってしまうことがある。ユーザーが実データテストで「クレーム・個人情報・団体名・危険情報が軒並み検知されなくなった」と報告した際、原因はコード側の不具合ではなく、この種の状態の持ち越しだったと判明（完全ログアウトでは再現しなかった）。分析結果一覧を失う「ログアウト」までしなくても、分析条件だけをクリーンな状態に戻せるよう、ユーザーの提案でこのボタンを追加した。
- **サイドバー最上部の表記**（2026-08-19変更）：タイトルを「After Coder」→「アフターコーディング支援ツール After Coder」、サブタイトルを「by Marketing Junction」→「KOTONOHA by Marketing Junction」に変更。
  - **同日追加調整**：日本語部分（アフターコーディング支援ツール）と「After Coder」で見た目のバランスが悪いという指摘を受け、`.sidebar-title-jp`（15px、1行に収まるサイズ）／`.sidebar-title-en`（20px、日本語よりやや大きめ）という専用CSSクラスに分離。あわせて`.sidebar-title-wrap`（`margin-top: -20px`）で見出しブロック全体を`st.logo()`のマーク側に近づけた。
  - **同日さらに追加調整**：マークとの間隔を0.5行分広げる方向に戻し（`margin-top: -20px`→`-10px`）、`.sidebar-title-jp`のサイズはそのまま色だけ白（`#FFFFFF`）に、`.sidebar-title-en`はサイズを2倍（20px→40px）に変更。**サイドバーの既定背景色は白系のため、白文字のJP行が背景に埋もれて見えなくなる可能性がある**（実機確認前の暫定対応、ログイン画面より先が実機確認できない制約のため、ユーザーの目視確認待ち）。「After Coder」の色は`#F9C1CF`→`#72C6EF`（水色、既存の`.main-title`等で使われる`#2E5C8A`と同系統）に変更。さらに、サイズ変更（40px）が反映されないという報告を受け、サイドバー内の`<p>`要素にはStreamlit側のより詳細なセレクタが優先される（デフォルトでサイドバーのタイポグラフィが縮小される仕様のため）と判断し、`.sidebar-title-jp`/`.sidebar-title-en`の`font-size`に`!important`を付与して強制的に上書きするようにした。ユーザーの実機確認で反映を確認済み。

---

## 3. 作業準備

| 項目 | 内容 |
|---|---|
| APIキー | Anthropic APIキーをテキスト入力（`password`型、画面には表示されるがマスクされる）。セッションを跨いでは保存されない。 |
| プロジェクト名（UI表記、内部変数名は`q_name`のまま。2026-08-19に「設問名」から改称） | 分析対象の自由回答設問を表す識別名。LLMへの全プロンプトに埋め込まれる。 |
| 分析データの特徴 | 調査背景・対象者などの自由記述。空でも動作するが、記入すると精度が上がる（特に方式B・C1〜C3で参照）。 |
| データアップロード | ①テキストファイル（.txt）／②テキスト直接貼り付け／③Excelファイル（ID・属性列あり）、の3方式。①②は1行＝1回答、③は列マッピングで対応づける（4.0節参照）。**2026-08-19、メインエリア左カラム「📋 作業メニュー」内の「📁 ファイルアップロード」折りたたみに格納（5.4節）**。中身・挙動は変更なし、置き場所のみ変更。 |
| コード数の上限 | 10〜49の範囲でスライダー指定。生成されるコードの総数の上限（詳細は6節「既知の制約」参照）。 |

### データクリーニングの実態（現状はごく簡易）

- テキストファイル読み込みは `content.decode('utf-8', errors='ignore')` — **UTF-8以外（Shift-JIS等）のファイルは文字化け・文字欠落する可能性があるが、エラーにはならず気づきにくい**。
- 行ごとに `strip()` して前後の空白を除去し、空行は除外する（①②のみ。③Excelは空欄の自由記述行も除外せず残す。4.5.4節の「無回答」判定が空欄テキストを検知する仕組みと噛み合わせるため）。
- **重複回答の除去・最小文字数フィルタ・文字コード自動判定などは行っていない。**

### 4.0 内部データモデル（`items`）とExcelアップロード（列マッピング）

2026-08-05、実際のアンケートRAWデータ（回答者ID・FA番号・年代や職位などの属性列を含む）を扱えるようにするため、内部のデータモデルを拡張した。

- **`items`の形**：`{'id': str, 'text': str, 'fa_no': str|None, 'attrs': dict}`のリスト。以前は`run_pipeline`内部で`texts`（文字列のリスト）から`{'id': 'NO001', 'text': ...}`を機械的に組み立てていたが、この構築処理をUI層（アップロード処理）に引き上げ、`run_pipeline(api_key, q_name, items, ...)`は組み立て済みの`items`をそのまま受け取るようになった（`_items_from_texts()`ヘルパーが①②の変換を担う）。
- **①テキストファイル／②貼り付け**：従来通り`id`は`NO001`形式で自動採番、`fa_no`は`None`、`attrs`は空辞書。挙動・出力とも従来と完全に同じ。
- **③Excelファイル（新規）**：`pd.read_excel()`でアップロードされたファイルを読み込み、列見出しをプレビュー表示した上で、ユーザーが4つのプルダウン／複数選択で列の役割を指定する：自由記述の列（必須）、回答者IDの列（任意、未指定なら`NO001`形式で自動採番）、FA番号の列（任意）、属性として使う列（複数選択・任意、**列数は固定せず可変**。選んだ列の見出し名がそのまま属性名になる）。列構成はファイルごとに異なりうる前提（実際の質問文が列見出しになっている等）のため、列名からの自動判定ではなく**手動マッピング**にしている。「この内容で読み込む」ボタンを押すと`items`が構築され、`st.session_state.xlsx_items`に保持される。
- **属性（`attrs`）はコーディング判断に一切使わない**：`llm_code_batch`のプロンプト・スキーマには含まれない。AIへの問い合わせ内容・トークン数・コストに影響しない。属性は最終的なExcelレポート表示のためだけにPython内部でid/textと一緒に運ばれる付随データ（自由回答一覧シートでの利用は今後の課題）。
- **`run_pipeline`内の`random.shuffle`**：受け取った`items`をそのまま`shuffle`すると呼び出し元のリスト（`st.session_state.xlsx_items`など）が意図せず並び替わってしまうため、`all_items = list(items)`でコピーしてからshuffleする。
- **コーディング処理・`create_excel`は変更不要**：コーディング側（現在は4.6.1節の`coding_job`）は`result['items']`をスライスするだけなので`fa_no`/`attrs`は自動的に引き継がれる。`create_excel`は現状`item['id']`/`item['text']`のみを読むため、余分なキーがあっても無視されるだけで既存の2シートの出力に影響はない。

---

## 4. Codebook Builder（コードブック策定）

サイドバーの「コードブック策定方式」で7つのモードから選択する。既定選択（`index=`）は**方式C-自動**。

| モード | 内部コード | 概要 |
|---|---|---|
| 方式A：標準 | `A` | 先頭30件（ランダム抽出後）でコードブックを生成し、残り全件を20件ずつ差分検出 |
| 方式B：トップダウン＋ボトムアップ | `B` | 「分析データの特徴」から想定コードの骨格を先に生成→先頭30件で具体化・定義付与→残りを差分検出。**「分析データの特徴」が空の場合は自動的に方式Aにフォールバック** |
| 方式C1：全件精査（1/4抽出） | `C1` | 全体の1/4から主題抽出→統合し初期コードブックを作成、残り3/4を差分検出 |
| 方式C2：全件精査（ハーフ抽出） | `C2` | 全体の1/2から同様に初期コードブックを作成、残り1/2を差分検出 |
| 方式C3：全件精査（フル抽出） | `C3` | 全件から主題抽出→統合。差分検出ステージなし（Stage1のみで完結） |
| **方式C-自動：全件精査（件数自動調整）（推奨）** | `C_AUTO` | 方式C1〜C3と同じStage1/Stage2の仕組みを流用しつつ、抽出比率を固定値ではなく件数に応じて`_auto_sample_ratio(N)`で動的に計算する（4.2.2節） |
| 既存のコードブックを使用（アップロード） | `EXISTING` | 生成処理を全てスキップし、アップロードされたコードブック（CSVまたはJSON）をそのまま使う |

2026-08-05、方式選択の見直しを実施：それまで既定・推奨だった方式B（トップダウン＋ボトムアップ）は、実データテストで「集計時にフラグが立たないコード（一度も回答が紐付かないコード）が約10%発生する」ことが分かり、精度面での不信感につながるとの理由で**推奨から外した**（選択肢としては維持。「手軽で精度も悪くない」位置づけ）。一方、方式C1〜C3（全件精査系）はいずれも良好だったため、C1〜C3の固定比率（1/4・1/2・フル）を、データ件数に応じて自動調整する新方式C-自動を追加し、これを新しい既定・推奨とした。

**コードID命名規則**：カテゴリIDは`CAT01`形式、コードIDは`C0101`形式（接頭辞は英字1文字の「C」のみ）。コードブック生成・差分検出・チャット編集の全プロンプトでこの形式を明示している（過去に接頭辞が「CO」のように2文字になって出力されることがあったため、「COのように2文字以上にしない」という否定形の指示を全プロンプトに追加した）。

### 4.1 差分検出の共通ロジック（`_diff_detect_loop`）

方式A・B・C1・C2で共通して使われる。既存コードブックに対し残りの回答を20件ずつバッチ処理し、「既存コードで対応できない新主題」があればコードを追加する。

**重要な制約**：コード数の合計が「コード数の上限」に達した時点で、**それ以降のバッチは一切処理されず**ループを抜ける。つまり上限が低い・データの多様性が高いと、後半のデータは新規コード検出の対象から外れる（全モード共通の制約）。

#### 4.1.1 コード数上限の強制（`_generate_codebook_step`の安全弁）

各生成プロンプト（`llm_generate_codebook`／`llm_generate_codebook_topdown`／`llm_elaborate_skeleton`／`llm_consolidate_topics`）には「コード総数は必ず{max_codes}個以内に収める（絶対に超えないこと）」という指示を入れているが、**これはプロンプト上の指示に過ぎず、AIが厳密に守るとは限らない**（実際に、上限15個を指定しても生成結果が20個を超えるケースが確認された。特に方式C系のStage1で抽出主題の多様性が高い場合に起きやすい）。

そのため`_generate_codebook_step`は、`EXISTING`（既存コードブックのアップロード）以外の全モードで、生成直後にコード総数をプログラム側でチェックする安全弁を持つ：

- 総数が上限を超えていなければそのまま返す（追加のAPI呼び出しは発生しない）。
- 超えていた場合のみ、既存の編集機能`llm_edit_codebook`を「内容が近いコード同士を統合し、必ず総数{max_codes}個以内に収めてください（絶対に超えないこと）」という指示とともに呼び出し、統合し直したコードブックに差し替える（1回のみ。統合後も超過している場合や呼び出しが失敗した場合は、元のコードブックをそのまま使う＝データを失わないフォールバック）。
- `EXISTING`モードはこのチェックの対象外（アップロードされたコードブックはユーザーが意図して用意したものとして、上限に関わらずそのまま使う）。

2026-08-10、ユーザーからの「コード数の上限を指定しても守られないことがある（15指定で20個以上になる）」という報告を受けて追加。

### 4.2 方式C1〜C3のステージキャッシュ

`_build_codebook_c` は Stage1（初期生成）／Stage2（差分検出）の結果を `st.session_state['stage_codebook_cache']` にキャッシュする。キャッシュキーは「回答内容（順不同）＋プロジェクト名＋分析データの特徴＋ratio」のハッシュ。**同一データで再実行した場合、既に成功したステージはやり直さず再利用する**（トークン節約のため）。

#### 4.2.1 Stage1内・主題抽出のディスクチェックポイント（大量データ対策）

**背景**：方式C3（`ratio=1.0`、全件精査）は差分検出（Stage2）を行わず、Stage1の主題抽出だけで全件を処理する。50件ずつバッチ処理するため、データ件数が多いと1回のボタンクリックで数十バッチ分のLLM呼び出しが連続して走る非常に長い処理になる。`st.session_state`はアプリの再起動やセッション切断で消えるため（Stage1途中でこれが起きると、それまでの抽出結果が全て失われ、ログイン画面に戻って最初からやり直しになる＝2447件規模のテストで実際に発生した不具合）、`st.session_state`より一段階頑丈な永続化が必要になった。

**対策**：`_build_codebook_c_stage1` は主題抽出のバッチが完了するたびに、進捗（`total_batches`／`completed_batches`／`topics_all`）を`.codebook_cache/`ディレクトリ配下のJSONファイル（`_stage1_cache_path`。ファイル名はcache_keyを再ハッシュした値）にディスク保存する。次回同じデータ（同一cache_key）で実行した際、`completed_batches`まで抽出済みとみなして未処理バッチから再開する。抽出が完了していれば統合（`llm_consolidate_topics`）まで一気に進み、統合が失敗した場合も抽出をやり直さずに済む。最終的な統合が成功したときのみこのチェックポイントを削除する（失敗時は残しておき、次回再開できるようにするため）。

- **既知の注意点**：cache_keyには`ratio`区切りの「:」が含まれるため、そのままファイル名に使うとWindows（NTFS）では特殊な意味を持ち、ファイルが実質空になる不具合があった（代替データストリームとして書き込まれてしまう）。そのためファイル名にはcache_key自体ではなく、それを再ハッシュした値を使っている。
- 放棄されたチェックポイントファイルは`_cleanup_old_stage1_checkpoints`により、実行のたびに古いもの（24時間超）から自動的に削除される。
- コーディング（`llm_code_batch`）は元々「100件でコーディング」のように区切って実行する設計のため、同種の問題は起きにくい（4.5節参照）。

#### 4.2.2 方式C-自動：Stage1抽出比率の自動調整（`_auto_sample_ratio`）

2026-08-05追加。方式C1〜C3は固定比率（1/4・1/2・フル）から手動で選ぶ必要があったが、方式C-自動はデータ件数`N`から目標抽出数`f(N)`を自動計算し、その比率（`f(N)/N`）を`_build_codebook_c`にそのまま渡す（Stage1/Stage2の処理自体はC1〜C3と完全に共通）。

```
f(N) = min(N, 10√N, 1000)
```

- **N ≤ 100**：`f(N) = N`（全部読む。`N ≤ 10√N`は`N ≤ 100`のときのみ成立するため、この閾値で自動的に切り替わる）
- **100 < N ≤ 10,000**：`f(N) = 10√N`（件数が増えるほど抽出**比率**は下がっていく＝平方根スケーリング。標準誤差がサンプルサイズの平方根に反比例するという統計的な考え方に基づく）
- **N > 10,000**：`10√N ≥ 1000`となるため`f(N) = 1000`で頭打ち（大量データでもStage1の処理時間・コストが際限なく増えないようにする上限）

ユーザーの実測で方式C1（1/4）・C2（1/2）・C3（フル）はいずれも精度が良好だったため、この式は特定の`N`でそれらの比率と自然に一致するよう設計されている（例：N=1600で`10√N=400`＝C1の1/4相当、N=400で`10√N=200`＝C2の1/2相当）。C1〜C3は固定比率の選択肢として引き続き残し、方式C-自動は「都度どの比率を選ぶべきか迷わなくてよい」既定・推奨の選択肢という位置づけ。

### 4.3 主題数が多い場合の縮約処理（C1〜C3・C-自動のStage1のみ）

Stage1で抽出した主題（トピック）の重複除去後の件数が300件を超える場合、200件ずつのチャンクに分けてLLMに類似表現をまとめさせ（`llm_reduce_topics`）、件数がそれ以上減らなくなるまで繰り返してから最終統合（`llm_consolidate_topics`）に渡す。

### 4.4 キーワード生成について（既知のギャップ）

**キーワードが付与されるのは方式C1〜C3・C-自動のStage1（`llm_consolidate_topics`）で生成された初期コードのみ。** 以下のコードにはキーワードが一切付与されない：

- 方式A・Bの初期コード（`llm_generate_codebook` / `llm_elaborate_skeleton` のスキーマにkeywordsフィールドが無い）
- 全モード共通の差分検出で追加されたコード（`llm_detect_new_codes` のスキーマにもkeywordsフィールドが無い。C1〜C3・C-自動のStage2で追加された分も含む）

キーワードを全コードに持たせたい場合は追加対応が必要。

### 4.5 コーディング範囲の段階選択（`コーディング範囲`セレクトボックス）

**全モード共通で**（既存コードブック読み込みも含む）、サイドバーの「🧮 コーディング範囲」（セレクトボックス。「コードブック策定方式」4節の選択肢も同様にセレクトボックス化されている。どちらも元はラジオボタンだったが、選択肢を常時表示する必要がないため画面をコンパクトにする目的でプルダウンに変更した）で3段階から選ぶ：

| 選択肢 | 内部値 | 動作 |
|---|---|---|
| 100件でコーディング（**デフォルト**） | `100` | 策定後、先頭100件（無ければ全件）をコーディング |
| 200件でコーディング | `200` | 同上、200件 |
| 全件コーディング | `None`→全件数 | 全件コーディング |

**「コーディングしない（策定のみ）」は2026-08-19にこの選択肢から削除した**（メインエリアの「作業メニュー」再編、5.4節参照。策定のみの実行は、この選択肢を経由せず「📐 コードブックの作成」ボタンが専任で担うようになり、両者が同じ役割を兼ねる重複状態を解消した）。以前は「コーディングしない」がこのセレクトボックスの選択肢の1つで、`0`を選んだ状態で「🚀 分析開始」ボタン（当時は1つだけ）を押すと策定のみが実行されていたが、今は「コードブックの作成」ボタンが常にコーディングなし（内部的に`0`固定）で実行され、このセレクトボックスは「作成とコーディング」「コーディング（現在のコードブックを使用）」の2ボタンでのみ参照される。実行結果は`result['coded_count']`（コーディング済み件数）と`result['total_items']`（全件数）で管理され、この2つの大小関係で結果画面の表示内容が決まる（4.9節参照）。

### 4.5.1 コーディングモデルの選択（`コーディングモデル`セレクトボックス）

「🧮 コーディング範囲」の直下に、コーディング（分類作業）に使うモデルを選ぶセレクトボックスがある（`CODING_MODEL_OPTIONS`：Haiku 4.5／Sonnet 4.6、デフォルトはHaiku 4.5）。コードブック策定・編集は`CODEBOOK_MODEL`（Sonnet 4.6固定）のまま変わらない。

- **精度・価格比較の想定用途**：同じコードブック（CSV/JSONでダウンロード→「既存のコードブックを使用」で再読み込み）に対し、コーディングモデルだけを変えて2回実行し、「🕒 最近使用したプロジェクト」で出現件数やコストを見比べる使い方を想定している。
- **分析単位でモデルを固定**：選んだモデルはその分析の`result['coding_model']`に保存され、「▶現在のコードブックでコーディングする」（4.6節）でコーディングし直す際もこの保存された値を使う（サイドバーの選択を後から変えても、既存の分析には影響しない）。1つの分析の中でモデルが混在することはない。
- 結果画面には、コーディング済み（`coded_count > 0`）の場合にその分析で使われたモデル名を表示する（「🧮 この分析のコーディングモデル：〜」）。

### 4.5.2 コーディング判定の厳密度（`CODING_STRICTNESS_OPTIONS`セレクトボックス）

2026-08-10追加（9節「今後の計画」3番目の項目を実装）。「コーディングモデル」の直下に、コード付与の判定基準の厳しさを選ぶセレクトボックスがある：

| 選択肢 | 内部値 | `llm_code_batch`のsystemプロンプトへの追加内容 |
|---|---|---|
| 標準（**デフォルト**） | `standard` | 追加なし（従来通りの挙動） |
| 厳密（誤検出を避ける） | `strict` | 「コードの定義・キーワードに明確に一致する場合のみ付与してください。文脈からの推測だけでは付与しないでください。」 |
| 柔軟（見落としを避ける） | `lenient` | 「コードのテーマに関連する内容であれば、表現が異なっていても幅広く付与してください。文脈から示唆される内容も対象に含めてください。」 |

- **対象範囲はコード付与の判定のみ**：センチメント・回答分類（4.5.4節）・リスクチェック（4.5.3節）の判定基準には影響しない（それぞれ独立したルール文として付加されるため）。
- **分析単位で固定**：`coding_model`・`enabled_risks`と同じパターンで`result['coding_strictness']`に保存され、「▶現在のコードブックでコーディングする」（4.6節）でコーディングし直す際もこの保存された値を使う（サイドバーの選択を後から変えても、既存の分析には影響しない）。異なる厳密度で結果を比較したい場合は、コーディングモデルの比較（4.5.1節）と同様に、同じコードブックのまま設定を変えて分析を2回実行し、「🕒 最近使用したプロジェクト」で見比べる使い方を想定している。
- **背景**：ユーザーから「15件指定で17件生成される」というコード数上限の話に関連して、コーディング判定自体の基準を明示的に調整したいという要望があり実装。厳密・柔軟のどちらも、その方向に倒すこと自体はトレードオフ（厳密＝非該当が増える可能性、柔軟＝過剰付与が増える可能性）であり、精度が一様に上がるわけではない前提で用意している。

### 4.5.3 リスクチェック（`RISK_CHECK_OPTIONS`チェックボックス）

「コーディングモデル」の直下に、コーディング時に検知させたい項目を選ぶチェックボックスが4つ並ぶ（`RISK_CHECK_OPTIONS`で定義）：

| ラベル | key（内部フィールド名） | 一文字見出し（Excel用） | 検知対象（`hint`） |
|---|---|---|---|
| クレーム | `claim` | ク | 感情的で強いクレーム |
| 個人情報 | `personal` | 個 | 個人名・メールアドレス・電話番号・詳細住所 |
| 団体名 | `org` | 団 | 学校名・病院名・施設名としての具体的な固有名詞（例：八戸第3小学校、△△総合病院）が明記されている場合のみ該当。「校長と教頭と教務主導で」「ICT担当のパワハラ教員」「管理職３人」のように、役職・立場・職務内容を表す言葉だけで固有名詞が一切ない文は、絶対に該当としない。 |
| 危険情報 | `danger` | 危 | 犯罪予告、自死予告、強い恨み、特定民族への差別 |

2026-08-04、精度調整の一環でユーザーの依頼により複数回の見直しを実施：
- 住所情報（`address`）は独立した項目として廃止し、個人情報の検知対象に統合した（住所も個人を特定しうる情報という点で個人情報と同種と整理）。
- **1回目の見直し**：実データでテストしたところ、クレーム・団体情報の**過検知**（誤検知）が目立った。原因は、このアンケート自体が「学校現場の課題・不満」を尋ねる内容のため、AIがアンケートの主旨に沿った通常の批判的意見まで拾ってしまっていたこと（クレーム）、および「校長」「教頭」「学校」のような一般的な役職名・組織種別を、具体的な団体名と混同していたこと（団体情報）。ユーザーが実際の誤検知例（例：「文部科学省と教育委員会からの縛りで動けない」＝制度への冷静な批判、「教頭、教務」＝単なる役職名の列挙）を提示し、それをもとに除外条件を明示する形でhintを書き直した（「〜は含めない」「〜だけでは該当としない」）。
- **2回目の見直し（団体情報のみ）**：1回目の修正を反映して全件再コーディングしたが、依然として「教務」「ICT担当」「管理職」「学級担任」のような、1回目の除外例に含まれていなかった役職名で過検知が続いた。原因は、AIが「校長・教頭・学校・教育委員会という**特定の単語**は対象外」という個別事例だけを学習し、「役職名一般は対象外」という**ルールそのもの**を汎化できていなかったこと。対策として、個別の単語を列挙する書き方から「役職・立場・職務内容を表す言葉」という**カテゴリ全体**を除外する書き方に変更し、「絶対に」「内容にかかわらず」という強い言い切りを加えた。**教訓**：LLMへの除外指示は、具体例の列挙だけでは類似の未列挙語に汎化されないことがある。個別事例ではなく上位カテゴリで除外条件を書き、断定的な言葉遣いを使う方が効果的。
- **3回目の見直し（クレームのみ、1回目を差し戻し）**：クレームの1回目の除外条件つきhintを反映して再テストしたところ、検知件数は減ったが「質的に強いクレームだけが残った」という実感は特になく、単に件数が減っただけだった。ユーザーの判断：これはリスク探し（見逃しよりカバー範囲の広さを優先すべき用途）なので、多少広く拾う分には問題ない。除外条件つきの狭いhintより、1回目の見直し前の広い表現（`感情的で強いクレーム`）に差し戻した。**教訓**：除外条件を追加するとAIが検知範囲を狭めるが、狭めた結果が必ずしも「質の良い絞り込み」になるとは限らない（単純に件数が減るだけのこともある）。リスク検知のような「広く拾う方が安全」な用途では、多少の過検知を許容し、絞り込みすぎないほうが良い場合がある。
- **4回目の見直し（団体情報→団体名にラベル変更＋NG文例追加）**：2回目の修正（カテゴリ除外＋強い言い切り）を反映して全件再コーディングしても、全く同じ3つの誤検知パターン（「校長と教頭と教務主導で」「ICT担当のパワハラ教員」「校長が…学級担任に…」）が再現した。ユーザーが「団体情報というラベル自体がAIに『組織に関する情報全般』と広く解釈させているのでは」と指摘。ラベルを`団体情報`→`団体名`に変更し（`RISK_CHECK_OPTIONS`の`label`はプロンプト中で`- {label}: {hint}`の形で使われるため、ラベルの語自体もAIへの入力になる）、加えてhintを単語の列挙からユーザーが報告した実際の誤検知文そのものを引用した「NG文例」の形に書き直した（単語より文全体のパターンの方が伝わりやすいという仮説）。同時に固有名詞の例も実際にテストで正しく検知できた実例（「八戸第3小学校」）に差し替えた。

- **チェックした項目だけAIに問い合わせる**：未チェックの項目は`llm_code_batch`のプロンプト・スキーマに一切追加されない（問い合わせ自体をしないため、コスト増にはならない）。全部未チェックなら従来（センチメントのみ判定）と完全に同じ動作。
- **分析単位で固定**：選択した項目は`coding_model`と同じパターンで`result['enabled_risks']`に保存され、「▶現在のコードブックでコーディングする」（4.6節）でコーディングし直す際もこの保存された値を使う（1つの分析の中でチェック対象が混在することはない）。
- 各回答のコーディング結果（`results`内の各要素）には、有効な項目についてのみ`{key}: true/false`が付与される（例：`{'id': 'NO001', 'codes': [...], 'sentiment': 'neutral', 'claim': false, 'personal': true}`）。無効な項目のキーはそもそも存在しない。
- 集計は`aggregate_results(..., risk_keys=enabled_risks)`が担い、`risk_counts`（`{key: 該当件数}`）を返す（5.3節参照）。
- 画面表示は5.4節の「📌 特記情報」を参照。Excelレポートへの反映は5.5.2節（特記情報集計・フラグ列）を参照。

### 4.5.4 回答分類（`ANSWER_TYPE_OPTIONS`、固定・常時判定）

2026-08-04追加。センチメント・非該当と同じ「固定」区分の分類で、チェックボックスはなく常に判定される：

| ラベル | key | 一文字見出し | 判定方法 | 検知対象 |
|---|---|---|---|---|
| 不明 | `unclear` | 不 | AI判定（`llm_code_batch`が常にスキーマ・プロンプトに含める） | 特になし・とくになし・なし・わからない・答えたくない・意味の不明な文字入力 |
| 無回答 | `unanswered` | 無 | ルールベース（`_code_one_batch`が回答テキストの空欄チェックで機械的に判定） | 空欄 |

- **無回答はAIに問い合わせない**：回答テキストが空欄かどうかは`_code_one_batch`が`llm_code_batch`の呼び出し後にバッチ単位で判定し、`res['unanswered']`として結果へ直接書き込む（コスト・精度の両面でAI判定より確実なため）。
- **無回答と不明の二重計上防止**：無回答（空欄）と判定された回答は、AIが誤って`unclear: true`を返していても`unclear`を`False`に上書きする（同じ回答が両方に計上されるのを防ぐ）。
- **不明はAI判定**：`ANSWER_TYPE_OPTIONS`の`llm=True`な項目（現状`unclear`のみ）だけを対象に、`llm_code_batch`が【回答分類】セクションをプロンプトへ常に追加し、対応するスキーマフィールドを`required`に含める（センチメントと同じ扱い＝AIが省略できない）。この仕組みは将来AI判定の項目が増えても`ANSWER_TYPE_OPTIONS`に`llm: True`で追加するだけで対応できる。
- 集計は`aggregate_results`が`answer_type_counts`（`{key: 該当件数}`）として返す（5.3節参照）。
- 画面表示・Excelレポートへの反映は、リスクチェックと同様に5.4節・5.5.2節を参照（特記情報集計・フラグ列では「非該当」の直後、リスクチェック項目より前に「不明」「無回答」が並ぶ）。

### 4.6 現在のコードブックでコーディングする（1ボタン、常に最初からやり直す）

**2026-08-19、ホームタブにあった専用ボタンは削除し、作業メニュー（5.4節）の「🧮 コーディング（現在のコードブックを使用）」ボタンに一本化した**（全く同じ処理が2箇所に重複していたため）。以下の設計意図・挙動はボタンの置き場所が変わっただけで、そのまま有効。編集案（`pending_edit`）が未確定の間はコーディング系ボタンを使わせない制約も、作業メニュー側の`coding_ready`判定を通じて維持している（4.10節）。

- **対象件数`N`**：`coded_count`（現在コーディング済みの件数）に関係なく、**サイドバーの「コーディング範囲」（4.5節）で指定した件数**（`coding_sample_size`。「全件コーディング」選択時は`total_items`）で決まる。
- 押すと `_start_coding_job('recode', ...)` を呼び出し、**既存の結果を破棄して、対象件数を現在のコードブックで最初からコーディングし直す**（「未コーディング分だけ追加」という部分継続の仕組みは廃止した）。実際のバッチ処理は4.6.1節の中断可能なジョブとして進む。
- **設計意図（2026-08-10、ユーザー要望により2ボタン制から1ボタン制に変更）**：想定している使い方は、①コードブック編集後の効果検証（例：左ナビを「100件でコーディング」にしたまま、編集→ボタンを押す→結果比較、を繰り返す）と、②コードブックが確定した後、左ナビを「全件コーディング」にして最終集計を行う、の2段階。旧・「残りN件」ボタン（未コーディング分だけ追加）は廃止した。理由は、部分継続だと「古いコードブックで処理済みの回答」と「新しいコードブックで処理した回答」が1つの結果内に混在してしまい、最終成果物としての一貫性が崩れるため（コードブックを編集するたびに全件を洗い替える方が、コストは増えても最終的な整合性を優先すべきというユーザー判断）。

押した時点で確定しているコードブック（4.10節参照）が使われる。トークン使用量は毎回累積される。

### 4.6.1 コーディング処理の中断（`coding_job`、バッチ単位の再実行モデル）

2026-08-10追加。コーディング処理（分析開始ボタンでの初回コーディング／4.6節の「現在のコードブックでコーディングする」）は、**バッチ（15件）単位で1回のスクリプト再実行に区切って進める**ことで、実行中に中断できるようにしている。

- **なぜバッチ単位に区切る必要があるか**：Streamlitは1回のスクリプト実行が完了するまで次の操作（ボタンクリックなど）を処理しない。従来のように全バッチをfor文で一気に処理すると、処理中に「中断する」ボタンを押しても反応しない。1バッチ処理するたびに`st.rerun()`でスクリプトを終了・再開すれば、バッチの合間（実際のAPI呼び出しで数秒かかる）に中断ボタンのクリックがブラウザから送られ、次の再実行の冒頭で検知できる。
- **状態管理（`st.session_state.coding_job`）**：ジョブの進行状態（`kind`＝`'initial'`／`'recode'`、対象`items`、`batch_index`、`results`、コーディングモデル・リスクチェック・厳密度の設定、更新先の`history_id`、`stop_requested`フラグなど）をセッション状態に保持する。ジョブが存在する間、画面はメインタイトル直下で進捗バーと「⏹ 中断する（それまでの結果を保存）」ボタンだけを表示し（`_render_coding_job()`）、それ以外のUI（サイドバー・結果表示など）は描画しない（`st.stop()`）。
- **`_start_coding_job(kind, ...)`**：ジョブを初期化してセッション状態にセットする。`kind='initial'`（分析開始ボタン）はコードブック策定を同期実行した直後に呼ばれるため`reset_usage=False`（策定分のトークンを維持）、`kind='recode'`（4.6節のボタン）は独立したジョブとして`reset_usage=True`・`prior_usage=`直前までの累積使用量、で呼ぶ。
- **`_render_coding_job()`**：呼ばれるたびに1バッチだけ処理して`st.rerun()`する。中断ボタンが押されている、または全バッチが終わっていれば、その時点までの`results`で`_finalize_coding_job()`を呼んでジョブを終了する。
- **`_finalize_coding_job(job)`**：`job['results']`（中断時は途中まで）を`aggregate_results()`で集計し、`job['history_id']`に一致する`st.session_state.history`のエントリへ`update()`で反映する。`coded_count`は実際に処理できた件数になる（中断すればその分だけ少なくなる）。
- **中断時の挙動**：それまでにコーディング済みの分は通常の部分コーディング結果と同様に扱われる（4.9節の`coded_count < total_items`の表示ロジックがそのまま適用される）。中断後に同じボタンをもう一度押すと、4.6節の設計どおり最初からやり直す（中断した続きから再開する機能ではない）。
- **対象範囲はコーディング処理のみ**：コードブック策定（`_build_codebook_and_codes`）は従来通り同期実行のままで、中断はできない（策定は通常1〜数回のAPI呼び出しで完了し時間がかからないため、対応の優先度が低いとユーザーが判断）。
- 旧・`continue_coding()`関数（バッチをfor文で一気に処理していた）は削除し、`_start_coding_job`/`_render_coding_job`/`_finalize_coding_job`に置き換えた。バッチ処理そのもの（LLM呼び出し＋無回答のルールベース上書き）は`_code_one_batch()`として切り出し、同期版の`_code_items()`（`coding_sample_size == 0`の策定のみパスで`run_pipeline`から呼ばれる。この場合は空リストに対して即座にreturnするだけで実質何もしない）とジョブ版の両方から共有している。

### 4.6.2 精度診断（`diagnostic_job`、標準/厳密の突合によるコードブック見直し提案）

2026-08-11追加。ユーザーが実データで標準/厳密の判定結果を比較したところ、境界線上の回答について「どちらのコードにも該当する/しない」ではなく「意味的に隣接する2つのコードのどちらを選ぶか」がスライドするケースが多いことが判明した（5.1節の定義文全文化の修正と合わせて発見）。この現象を自動で検出し、コードブックの見直し案を作成する機能。

- **▶ 🎯 精度診断を実行（N件を標準・厳密の両方でテスト）**：**2026-08-19、🔧コードブック編集タブの「①精度診断」セクションへ移設**（以前はホームタブの「現在のコードブックでコーディングする」ボタンの直下にあった。詳細は4.10節）。Nは左ナビの「🎯 精度診断のテスト件数」スライダー（4.5.2節の「コーディング判定の厳密度」の直下、20〜300件・デフォルト100件、通常のコーディング範囲とは別設定）で決まる。編集案（`pending_edit`）が未確定の間は表示しない。
- **処理の流れ（`diagnostic_job`、4.6.1節の`coding_job`と同じくバッチ単位の再実行モデル）**：
  1. 対象N件を**標準**でテストコーディング（バッチごとに`st.rerun()`、中断可能）。
  2. 続けて同じN件を**厳密**でテストコーディング。
  3. 両方の結果を回答IDで突き合わせ、`_diagnose_confused_pairs()`が「同じ回答で、標準側にしかないコードと厳密側にしかないコードが両方存在する」ケースを集計し、頻度上位10ペア（コードの組み合わせ単位）と、ペアごとの回答例（最大3件）を抽出する。
  4. 検出したペア・定義・回答例から`_build_diagnostic_instruction()`が指示文を組み立て、**既存の`llm_edit_codebook()`をそのまま再利用**して見直し案（意味が重なるペアは統合、区別する価値があるペアは判断基準を明記して定義文を書き直す）を作成させる。新規のLLM関数は追加していない。
  5. 見直し案は`result['pending_edit']`にセットし、**4.10節の編集案プレビュー・確定/キャンセルの仕組みをそのまま流用**する（専用のUIは作っていない）。混同ペアが1件も見つからなければ、その旨を`st.info()`で1回だけ表示する（`result['diagnostic_message']`、表示後は`pop()`で消費して消える）。
- **リスクチェックは含めない**：診断用のテストコーディングは`enabled_risks=[]`固定で呼ぶ。コード判定の混同とは無関係な項目のため、コスト・焦点をコード判定だけに絞る。
- **中断の挙動が4.6節と異なる**：中断すると診断全体を中止し、それまでの部分結果は使わない（見直し案は作らない）。標準/厳密2フェーズにまたがる複合ジョブのため、部分データからの見直し案は根拠が薄くなりミスリードのリスクがあると判断し、単純化した。
- **コスト**：通常のコーディングに加えて「テストコーディング×2＋見直し案作成1回」が追加でかかる、明確な追加コストのオプション機能として位置づけている（サイドバー・ボタンとも常時表示だが、実行するまでコストは発生しない）。

### 4.7 コードブック（構造＋GT数値、常時展開）

コードブックの構造（件数・出現率(%)・カテゴリID・カテゴリ名・コードID・コード名・定義・キーワード）は `render_codebook_structure()` によって**1つの表に統合し、折りたたまずに常時表示**する（2026-08-10、インターフェイス整理の一環として旧・別表「コード一覧集計」を廃止し統合。旧設計では編集判断の際に集計値が紛らわしいという指摘を受けて意図的に分離していたが、実際には2つの似た表が並ぶこと自体が複雑さの原因になっていたため、ユーザーの要望で再統合した）。編集案を確定した直後もその場で最新の内容がそのまま反映される（展開操作は不要）。

- 件数・出現率(%)は`gt_by_code`（コード別GT集計、コーディング未実施時や新規追加コードは0）を引数で渡し、**表の最左列**に表示する。
- 列順：件数・出現率(%)・カテゴリID・カテゴリ名・コードID・コード名・定義・キーワード。
- キーワード列はリスト表記ではなく `丁寧; 親切` のようにセミコロン区切りの文字列として表示・出力される。
- 表右上のツールバー（Streamlit標準機能）から**CSVとしてダウンロード**できる（専用のダウンロードボタンは無い）。件数・出現率の列もそのまま含まれてダウンロードされる（ダウンロード専用に数値を除いた版は用意していない）。ダウンロードしたCSVは「既存のコードブックを使用」で再読み込みできる（`parse_codebook_csv()`が件数・出現率列を無視するため、実害はない）。
- `st.dataframe` ではなく `st.data_editor`（disabledにせず全列編集可能の状態）で描画している。セルをダブルクリックすると実際のテキスト入力欄が開き、定義文などの長いテキストを全文選択してコピーできる（`st.dataframe`はcanvas描画のグリッドで、ドラッグによる部分テキスト選択ができないため。コードブック編集時に定義文をコピーして参照したいという要望に対応）。**ここでの編集内容は保存されない**（戻り値を使っていないため、入力してもセッション状態やコードブックには一切反映されない。あくまでコピー用の表示手段）。呼び出し時は`key`引数で一意のキーを指定する（`codebook_current`／`codebook_pending`）。

### 4.8 既存コードブックの読み込み（`EXISTING`モード）

- CSV（4.7のCSVダウンロードと同一フォーマット）またはJSONをアップロードできる。**過去に生成・保存したどのバージョンのコードブックでも読み込める**（4.10の編集機能の入力としても使う想定）。
- CSVは `parse_codebook_csv()` で構造に復元する。文字コードは UTF-8（BOM有無問わず）／Shift-JIS（cp932、Excelで開いて保存し直した場合）の両方に対応。件数・出現率などコードブック構造に無い列は無視される。
- 読み込んだコードブックには「コード数の上限」は適用されない（アップロードされた件数がそのまま使われる）。

### 4.9 エラーハンドリング

コードブック生成に失敗した場合、`llm_client.get_last_error()` から直近の失敗理由（例外メッセージ、または「モデルが空の結果を返しました」等）を取得し、画面に「詳細:」として表示する。原因不明時は汎用メッセージにフォールバックする。

### 4.10 コードブック編集機能（`_render_codebook_edit_tab`、🔧コードブック編集タブに集約、2026-08-19）

コードブックの編集に関わる機能はすべて`🔧 コードブック編集`タブ（5.4節、3タブ構成の3つ目）に集約されている。**🏠ホームタブのコードブック表示は完全に表示専用**（`render_codebook_structure()`のみ、編集の入口は一切無い）——`pending_edit`が立っている場合は「📝 未確定の編集案があります。『🔧 コードブック編集』タブで確認してください」という案内だけを出す。

**画面構成（`_render_codebook_edit_tab(result, api_key, q_name, diagnostic_size)`、上から）**：
1. **現在のコードブック**（常時表示、表示専用。`render_codebook_structure()`）
2. **編集案のプレビュー＋確定/キャンセル**（`pending_edit`があるときだけ表示。以前ホームタブにあったものと全く同じUI・ロジックをそのまま移設——コードブック全体のプレビュー、`_diff_codebook()`による「🔍 変更点の詳細」、「✅ この内容で確定する」「❌ キャンセル」の2段階確認）
3. **① 精度診断**（4.6.2節。以前はホームタブにあったボタンをそのまま移設）
4. **② 自由チャット編集**（指示文を入力してAIに編集案を作らせる、`st.form`。**以前ホームタブから一時削除していた機能を、このタブへ復活させた形**——`llm_edit_codebook(client, codebook, instruction, q_name)`を呼ぶ点は変わらない）
5. **③ 統合候補・分割候補の処理**（詳細は下記。**2026-08-19、①②とは独立した専用フローに仕様変更**）

**①②は共通の`pending_edit`の仕組みを使う**（`result['pending_edit'] = {'instruction': ..., 'codebook': proposed}`をセットし、上記2のプレビューUIで確認・確定する）。そのため、**①②のどちらかで編集案が未確定（`pending_edit`が残っている）の間は、①②も③も実行できない**（`edit_blocked`フラグで一括判定。1度に1つの提案しか扱えない設計）。③は独自の即時確定フローを持つが、①②の`pending_edit`が残っている間は同様に③の提案作成・採用もブロックする（コードブックの前提が食い違ったまま複数の編集が並行しないようにするため）。

- **①②の編集を確定した後は、効果を確かめるために再コーディングが必要**（作業メニューの「🧮 コーディング（現在のコードブックを使用）」、5.4節、または`▶現在のコードブックでコーディングする`が果たしていた役割と同じ）。このタブ自体はコーディングを行わない——①②のセクション末尾にその旨のキャプションを表示している。

**③ 統合候補・分割候補の処理**（📋基本集計表タブの「🔗 統合候補」「✂️ 分割候補」から送られたコード、5.4.2節参照）：
- **仕様変更の経緯（2026-08-19）**：当初はここも①②と同じ「`llm_edit_codebook`でコードブック全体を編集→共通プレビューで確認→確定」という流れだったが、ユーザーから「対象コードの定義を提示し、その下に編集可能な統合案／分割案を示し、候補ごとに個別の採用/中止ボタンを付けてほしい」という仕様変更の依頼を受け、**コード単位で完結する専用フロー**に置き換えた。
- `result['edit_queue_merge']`／`result['edit_queue_split']`の中身を一覧表示する。各候補には、その候補が**作られた時点のコードブックのバージョン**（`origin_version`＝登録時の`len(result.get('edit_log', []))`）が記録されている。
- **有効性判定**：`codebook_version = len(result.get('edit_log', []))`（現在のバージョン）と`cand['origin_version']`を比較し、一致しなければ「⚠️ 使用不可（登録後にコードブックが変更されています）」と表示する（候補作成後にコードブックが①②で編集された、またはリビルドされた場合。ユーザー要望：「候補を作った時点とコードブックが変わっている場合は使えない」）。**候補自体は削除されず、「候補から削除」ボタンで手動削除できる**（無効化されたまま一覧に残る）。
- **対象コードの提示**：候補ごとに、統合なら対象2コード、分割なら対象1コードの**現在の名称・定義**を常に表示する（`result['codes']`から`code_id`で引く。ユーザー要望「対象となったコードの定義等を提示して」に対応）。
- **提案の生成**：「🤖 統合案を作成」／「🤖 分割案を作成」ボタンを押すと、候補専用の新しいLLM関数を呼ぶ（コードブック全体は渡さず、対象コードの名称・定義だけを渡す）：
  - `llm_propose_merge(client, code_a, code_b, q_name)` — 統合後の1コード（`code_name`/`definition`/`keywords`）を提案。
  - `llm_propose_split(client, code, q_name)` — 分割後の2コード（同上×2、`codes`配列で返す）を提案。
  - 生成結果は候補自身（`cand['proposal']`／`cand['proposals']`）に保持され、`result['pending_edit']`は使わない——③は①②の共通プレビューを経由しない。
- **編集可能な提案表示**：生成された提案は`st.text_input`／`st.text_area`（`value=`で提案内容を初期値にしたウィジェット、統合は1件・分割は2件）としてそのまま表示され、確定前に自由に書き換えられる（ユーザー要望「統合案は文字編集可能状態にしてください」に対応）。
- **候補ごとの「✅ 採用する」「❌ 中止する」**（2026-08-19、二段階目の仕様変更：**採用してもコードブックは即座には書き換わらない**）：
  - **採用する**：その時点でテキスト欄に入っている内容（編集されていればその内容）を`result['pending_codebook_changes']`（画面表示は「📝 コードブックへの反映待ち」）に積む（`{'kind': 'merge'|'split', 'remove_ids': [...], 'target_cat_id': ..., 'new_codes': [{'code_name','definition','keywords'}, ...], 'label': ..., 'source_names': [...]}`）。この時点ではコードブック本体もキャンセル・コードIDも未確定。候補自体はキューから削除する（採用済みのため、提案自体はもう不要）。
  - **中止する**：提案（`cand['proposal']`／`cand['proposals']`）だけを破棄し、候補自体はキューに残す（再度「🤖 提案を作成」からやり直せる）。候補を完全にリストから外したい場合は別途「候補から削除」ボタンを使う。
  - **経緯（仕様変更の理由）**：当初案（本節の初版）は「採用する」を押した時点で即座にコードブックへ反映していた。しかしそのたびに`codebook_version`（`edit_log`の件数）が進み、**まだ処理していない他の候補が軒並み「候補作成後にコードブックが変わった」と判定されて使用不可になり、統合と分割を続けて行う作業が進められない**という問題をユーザーが実機で発見（「統合作業を行ったあとに分割作業を行ったときの例です」）。この指摘を受け、採用＝コードブックへの反映を切り離し、複数の統合・分割をいったんすべて「採用」でステージングしてから、最後にまとめて1回で反映する方式に変更した。
- **「📝 コードブックへの反映待ち」セクション**（③の末尾）：`pending_codebook_changes`の各項目を、対象コード名→新コード名の要約（例：「🔗 統合：コードA＋コードB→統合コード」）で一覧表示する。項目ごとに「取り消す」ボタンでステージングから外せる（候補には戻さない。やり直す場合は再度候補登録から）。
- **「📝 コードブックを書き換える」ボタン**：`_apply_staged_changes(result, staged_changes)`を呼び、ステージング中の変更を**まとめて1回で**コードブックに反映する。
  - **重複コード名チェック（ユーザー要望「コード名の重複などはこの段階でチェックしたら」に対応）**：適用前に、今回追加される全コード名どうし、および削除されない既存コード名との重複を検査する。重複があれば`(None, 重複名のリスト)`を返して**何も変更せず**エラー表示のみ行う（ステージングは維持されるので、該当コード名を修正するか取り消してから再度実行できる）。
  - **コードID採番の衝突回避**：ステージング項目を1件ずつ順番に適用し、`_next_code_id()`を直前までの適用結果に対して都度計算し直すため、同じカテゴリへ複数件を同時に反映してもIDが衝突しない。
  - **`edit_log`の参照独立性**：`copy.deepcopy(result['codebook'])`から作った作業用コードブックを1件適用するたびに、そのスナップショットを`copy.deepcopy`してから`edit_log`に追記する。同じオブジェクト参照をそのまま複数のログエントリに使うと、後続の適用で過去のログまで一緒に書き換わってしまう（参照共有バグ）——実装時に発見し、単体テストで検証済み。
  - 統合は対象2コードの`cat_id`のうち最初のコードのカテゴリに、分割は元コードと同じカテゴリに新コードを追加する。
  - 反映後、`result['pending_codebook_changes']`を空にする。①②の`pending_edit`が残っている間はこのボタンも実行できない（`edit_blocked`で共通ガード。コードブックの前提が食い違ったまま複数の編集が並行しないようにするため）。
- **`_next_code_id(codebook, cat_id, extra_taken=())`**：指定カテゴリ内の既存コードID（`C0101`形式）から次の空き番号を採番する。同一カテゴリへの複数同時追加では、直前に採番した番号を`extra_taken`に渡して衝突を避ける。
- ③向けの内部データ：`result['edit_queue_merge']`／`result['edit_queue_split']`（📋基本集計表タブの「送る」ボタンが書き込む、5.4.2節）、候補ごとの`proposal`／`proposals`（生成された提案、未生成時は無し／`None`）、`result['pending_codebook_changes']`（採用済みだが未反映のステージング一覧）。
- `llm_edit_codebook(client, codebook, instruction, q_name)` は①②専用として引き続き使われる（③はもう使わない）。現在の呼び出し元は3箇所：①`_generate_codebook_step`のコード数上限超過時の安全弁（4.1.1節）、②精度診断（4.6.2節）、③自由チャット編集。
- 「コードブックを書き換える」で反映が成功するたびに、ステージング項目1件につき1件、`result['edit_log']`にバージョンを追記する（①②の確定と同じ仕組み）。**「📜 編集履歴」の一覧・巻き戻しUIは未実装のまま**（今後の課題）。
- **反映（書き換え）はコーディング済みの結果に自動反映されない。** 反映したい場合は作業メニューの「🧮 コーディング（現在のコードブックを使用）」（5.4節）を実行する必要がある。
- **既知の制限（今後の課題）**：ステージング中（採用済みだが未反映）の変更が使う予定のコードIDが、別の未処理候補からも参照されている場合の整合性チェックは行っていない（基本集計表タブの候補登録時点での「1コード1候補まで」制約はあるが、キュー送信後に同じコードが別経路で再度候補化されるケースまでは防いでいない）。実運用上は考えにくいため、2026-08-19時点では対応を見送っている。
- 2026-08-19、標準スクリプトで検証：構文チェック、`_next_code_id`（カテゴリ内採番・`extra_taken`による衝突回避・カテゴリ間の独立性）と`_apply_staged_changes`（統合＋分割の複数件同時適用でのID衝突回避、重複コード名チェック＝既存コードとの重複／新規コードどうしの重複の両方、`edit_log`の各エントリが独立したオブジェクトを保持していること＝参照共有バグが無いこと）の単体テスト、クリーンなサーバー起動でエラー無しを確認。実際のブラウザ操作によるE2E確認（AI呼び出しを含む）は、APIキーが必要なため未実施。

### 4.11 プロジェクトファイル（保存・再開、暫定サイドバー配置、2026-08-17追加）

RAWデータ・列マッピング・コードブック・コーディング結果を1つのJSONファイルにまとめて保存し、後で読み込んで同じ状態から再開できる機能。データの再アップロード・列マッピングのやり直しを繰り返さずに済むようにする目的で追加した。

**背景**：これは、右メインエリアを「作業」単位のメニュー構造に再編する、より大きな設計（`C:\Users\ryu\.claude\plans\after-coder-project-file-restructure.md`に設計合意を記録、未実装）の一部を先行実装したもの。大きな再構成が完了するまでの**暫定的な置き場所としてサイドバーに配置**しており、再構成時にはメインエリア上段「既存プロジェクトを開く」に移設する予定。

- **保存する内容**：`_build_project_file(result)` が、アクティブな分析結果（`result`）をそのままJSON化する。`pending_edit`（未確定の編集案）だけは、再開時に古い提案が残っていると混乱を招くため除外する。それ以外（`codebook`/`codes`/`items`/`results`/`gt`/`sent`/`usage`/`coding_model`/`coding_strictness`/`enabled_risks`/`answer_type_counts`/`coded_count`/`total_items`/`q_name`、確定済みの`edit_log`があればそれも）はすべてそのまま含む。
- **ファイル形式**：`{'app': 'after_coder_project', 'format_version': 1, 'saved_at': ISO日時, 'result': {...}}` という封筒構造。既存の「既存コードブックを使用」機能が読み込むJSON（`{'categories': [...]}`、4.8節）とは形が異なるため、誤って別の用途のJSONを読み込んでも判別できる。
- **ダウンロード**：サイドバー「💾 プロジェクトファイル」の`st.download_button`。`json.dumps(..., default=str)`を使い、Excelアップロード由来の`attrs`にpandas固有の型（numpy数値型など）が混じっていても、文字列化されてシリアライズに失敗しないようにしている。アクティブな分析結果が無い場合はボタンの代わりに案内文を表示する。
- **読み込み**：同じ場所の`st.file_uploader`＋「📂 この内容で開く」ボタン。`_load_project_file(payload)`が`app`タグと`result`内の必須キー（`codebook`/`items`）を検証してから返す。不正なファイルは`st.error()`でメッセージを表示し、何も変更しない。正常に読み込めた場合は、内部の分析結果一覧（`history`、2節参照）と同じ形（`{'id', 'q_name', 'timestamp', 'result'}`）で新しいエントリとして追加し、それをアクティブにして`st.rerun()`する（＝今のセッション内の一覧に、読み込んだプロジェクトが1件追加される形）。
- **サイドバー整理（2026-08-19）**：旧「📜 作業履歴」（直近10件表示）を「🕒 最近使用したプロジェクト」に改称し、表示件数を3件に絞った上で、この「💾 プロジェクトファイル」セクションの直下に統合した（プロジェクトファイルによる保存・再開が主要な導線になったため、長い履歴一覧を独立して常時表示する必要性が下がったという判断）。「📖 使い方」の案内と、本節の説明文（旧・サイドバー直書き）は🏠ホームタブの先頭（`📖 使い方・プロジェクトファイルについて`という折りたたみ）に移設した——**この移設は文言をそのまま移すだけの暫定対応で、レイアウト自体はホーム画面の見直し時に再調整する**。サイドバーには、ダウンロード/アップロードの操作UI本体のみが残る。
- 2026-08-17、標準スクリプトで7件のアサーション（`pending_edit`が除外されること、他のキーが保持されること、JSON化→読み込みのround-tripが一致すること、appタグ不一致・必須キー欠落のファイルをそれぞれ`ValueError`で弾くこと、非シリアライズ可能な値が`default=str`で文字列化されて例外にならないこと）により検証。クリーンなサーバー起動でエラー無し・ログイン画面までの表示を確認（ログインゲートより先はAPIキーが必要なため、既存の検証範囲の限界と同じ）。

---

## 5. After Coder（コーディング・集計）

### 5.1 コーディング（`llm_code_batch`）

- 15件ずつバッチ処理し、コードブックに基づいて各回答にコードを付与する。
- **マルチアンサー**：1回答に複数コードを付与可能。該当コードが無ければ空配列。
- **モデル**：サイドバーの「コーディングモデル」（4.5.1節）で選択した値（デフォルトは`CODING_MODEL` = `claude-haiku-4-5`）を使用する。コードブック策定・編集（`CODEBOOK_MODEL` = `claude-sonnet-4-6`固定）と違い、既存コードブックに沿った分類作業であるため、精度・価格のトレードオフを比較しながらモデルを選べるようにしている。
- **プロンプトキャッシュ**：コードブック本体とルール（1回のコーディング実行中は変化しない部分）を`system`側に分離し、`cache_control`（`llm_client.call_llm`の`cache_system=True`）でキャッシュする。回答本文（バッチごとに変わる部分）のみを`user`メッセージに置くことで、1回のコーディング実行で何十バッチも繰り返す際、2バッチ目以降はコードブック分の入力コストが約1/10になる。
  - キャッシュの最小適用サイズはHaiku 4.5で4096トークン。コードブックが小さい場合（コード数が少ない、定義が短いなど）はこの閾値に届かず、キャッシュが効かないことがある（エラーにはならず、無効化されるだけ）。
  - キャッシュはAnthropicのデフォルト5分TTL。バッチ処理は通常数分以内に完了するため、実行中は概ねキャッシュが維持される。
- **リスクチェック（`enabled_risks`）**：サイドバーでチェックされた項目（4.5.3節）のみ、`system`プロンプトのルールとスキーマに動的に追加される。プロンプトキャッシュ対象の`system`に含まれるため、追加コスト（トークン）は初回バッチ分のみで、2バッチ目以降はコードブック同様キャッシュの恩恵を受ける。回答分類・リスクチェックは以下の文単位化の対象外で、従来通り**回答単位**で判定する（後述）。
- **コーディング判定の厳密度（`strictness`）**：サイドバーで選択した段階（4.5.2節、デフォルトは`standard`）に応じて、`standard`以外の場合のみ【コード判定基準】ルールを`system`プロンプトへ追加する（`standard`は追加なし＝従来通りの挙動）。センチメント・回答分類・リスクチェックの判定基準には影響しない。
- **コード定義文は全文を渡す（2026-08-11修正）**：`system`プロンプトの【コードブック】セクション（`code_list`）に載せる各コードの定義文は、以前は`c["definition"][:25]`で**先頭25文字に切り詰めていた**。定義文は40〜100文字程度のものが多く、他の類似コードとの区別点が25文字より後ろに書かれているケースが多かったため、コードブック上は書き分けているつもりでも、AIが実際に読む範囲では区別できていなかった。ユーザーが実データで標準/厳密の判定結果を比較した際、意味的に近い2つのコード間でコードの選択がスライドするケースが目立ち（例：C0201「年功序列」⇔C0202「声の大きい人」）、原因調査の過程で発覚した。プロンプトキャッシュ（本節参照）により、`system`側は初回バッチ以降キャッシュされるため、全文化によるコスト増は初回バッチ分のみで軽微。

#### 5.1.1 文単位のコード判定（`_split_sentences`、2026-08-16追加）

**背景**：1セル（1回答）に複数の異なる話題を書く回答者が多く、これが①コーディング精度、②「回答例」の質（1つの長い回答が複数コードの実例として何度も繰り返し引用され読みにくい）、③センチメント判定（要素が混在する回答を1つの感情に無理に帰結させてしまう）の3点に悪影響を与えているという指摘を受けて導入した。検討の結果、**形態素解析（単語分割）ではなく文分割（文境界検出）** が必要な技術であると整理し、外部ライブラリを使わないルールベースの文分割で実装した。

- **`_split_sentences(text)`**：句点・感嘆符・疑問符（`。！？`）の直後で区切る正規表現ベースの文分割。区切り記号の無い末尾の断片も1文として扱う。空文字列・空白のみの断片は除外する。形態素解析（単語単位への分割）ではないため、MeCab等の外部ライブラリは不要。
- **コード判定は「回答全体を読みつつ、判定結果は文ごとに返す」方式**：`llm_code_batch`は各回答を`[1] 文1\n[2] 文2...`のように文番号付きでプロンプトに渡す。AIには**回答全体の文脈を踏まえた上で**判断するよう指示しつつ、返す結果は`sentences: [{idx, sentiment, codes}, ...]`という文単位の配列にする。これにより、「回答全体を俯瞰して判断する」という以前からの強み（1文だけを機械的に独立評価するのではなく、前後の文脈も加味できる）を維持したまま、コード・センチメントの判定粒度だけを文単位に上げている。
- **回答単位への集約（呼び出し元への後方互換）**：AIから返る文単位の生データ（`sentences`）は、`llm_code_batch`内でその場で回答単位の値に集約し、`codes`（全文の和集合）・`sentiment`（後述）という**従来と同じ名前・同じ形のフィールド**として`results`の各要素に埋め込む。これにより、`aggregate_results`・画面表示・Excelの既存ロジック（集計レポート／集計ダイジェスト／回答別コーディング結果）は**一切変更せずに動作する**。文単位の生データは追加のフィールドとして温存され、5.5.5節の新シートと自由回答一覧（5.5.4節）の該当箇所抜粋に使われる。
- **根拠は文番号（idx）で返させ、テキストはPython側で埋め込む**：AIには文そのものを書き写させず、番号だけを答えさせて、`sentences_by_id`（プロンプト構築時に作った文リスト）から`text`をPython側で引き当てる。これにより、原文と完全に一致する引用が保証される（AIによる言い換え・要約のブレが原理的に発生しない）。
- **`key_sentence_idx`**：回答ごとに、回答者が最も伝えたいこと・結論と言える文の番号を1つ追加で答えさせる（無ければ0）。文番号から本文を引き当て`key_sentence`として埋め込む。センチメントやコード判定とは独立した、レポート用の要約情報。
- **回答分類・リスクチェックは対象外**：不明・無回答・クレーム・個人情報・団体名・危険情報は、従来通り回答単位で1回だけ判定する（文単位にはしない。ユーザーの明示的な選択）。
- **コストへの影響**：文ごとにコード・センチメントを返させる分、出力トークンは回答内の文の数に比例して増える。コードブック部分はキャッシュされたまま変わらないため、増分は純粋に出力側のみ。
- **AIが返す`id`は信頼せず、件数が一致すれば位置で付け直す（2026-08-16、実データで発覚した重大バグの修正）**：文番号付きの複数行プロンプト（`ID:\n[1] 文\n[2] 文...`）に変更して以降、実際のAPI呼び出しで**AIが返す`id`が元の`items`の`id`と一致しない**ケースが実データで多発することが判明した。1行形式（`ID: 文`）のときは問題なかったため、複数行にまたがる形式になったことでAIが英数字IDの正確な書き写しを崩しやすくなったとみられる。この不整合は、コード・センチメント自体（AI自身が返した`sentences`から機械的に集約するのでidに依存しない）には影響しないが、**`id`をキーに元の`items`と突き合わせる処理を全て壊す**：`_code_one_batch`の無回答判定（`batch_text.get(r['id'], '')`が空文字列にフォールバックし、実際は回答があるのに大量に「無回答」と誤判定される）、および`create_excel`の`item_map_full.get(res['id'])`に依存する自由回答一覧・文単位データシート（該当する`item`が見つからず該当行が1件も生成されない）。ユーザー提供の実データ（100件、Haiku 4.5）で、無回答フラグがほぼ全件に誤って立ち、自由回答一覧・文単位データが共に0行という形で発覚した。**対策**：`llm_code_batch`が返した結果の件数が送った`items`の件数と一致する場合、AIが返した`id`文字列を使わず、**送った順序に基づいて機械的に`items`の`id`を割り当て直す**（`zip(items, raw_results)`）。件数が一致しない場合（AIが回答を欠落・重複させた場合）は付け替えができないため、従来通りAIが返した`id`にフォールバックする（この場合のみ、本バグと同種の突き合わせ失敗が理論上再発しうる）。

### 5.2 センチメント分析（文単位判定＋回答単位への集約）

センチメントは5.1.1節の仕組みに合わせて**文ごとに** `positive` / `negative` / `neutral` のいずれか1つ（シングルアンサー）を判定する。回答単位の`sentiment`は、その回答に含まれる全文の判定結果から`llm_code_batch`が機械的に導出する（AIに「回答全体としてどうか」を別途判断させるのではなく、内訳から計算する方が一貫性が高いという判断）：

- 全文が同じ値 → その値をそのまま採用
- 文の判定が割れている（例：一部の文はnegative、別の文はpositive） → `mixed`（**混在**）
- 文が1つも無い（無回答など） → `neutral`

`mixed`は`positive`/`negative`/`neutral`と並ぶ4つ目のセンチメント区分として、`sent_counts`・画面の「📌 特記情報」・Excelの「特記情報集計」に表示される（5.3節・5.4節・5.5.2節参照）。**背景**：ユーザーから「様々な要素が混在するヘビーな回答が1つのセンチメントに強引に帰結させられるのはおかしい」という指摘があり、単純な5段階の強弱スケールではなく、「そもそも複数の異なる感情が同居している」ことを正直に記録できる区分として導入した。

### 5.3 集計（`aggregate_results`）

- コード別GT集計（出現件数・出現率）
- センチメント集計（ポジティブ／ネガティブ／ニュートラル／**混在**の件数・割合。`sent_counts`は4区分、5.2節参照）
- **非該当**：どのコードも付与されなかった回答の件数・割合（センチメントとは独立した指標。画面・Excel両方に表示）
- **回答分類集計（`answer_type_counts`）**：`ANSWER_TYPE_OPTIONS`（4.5.4節、不明・無回答）の各項目について、`results`内の対応フィールド（`unclear`/`unanswered`）が真の件数を常に算出する（リスクチェックと異なり、チェックボックスによる有効/無効の切り替えはない＝常時計算）。
- **リスクチェック集計（`risk_counts`）**：`risk_keys`（＝サイドバーで有効化された項目、4.5.3節）が指定された場合のみ、項目ごとの該当件数を算出する。無指定（`risk_keys=None`）時は空辞書を返し、既存の呼び出し元との後方互換を保つ。
- 戻り値は`gt, sent_counts, unassigned, risk_counts, answer_type_counts`の5要素タプル（2026-08-04、`answer_type_counts`追加により4→5要素に変更）。
- 分母は**コーディング済み件数**（`coded_count`）。全件未コーディングの部分連続実行時は、コーディングした範囲内での割合になる。

### 5.4 画面表示

**メインエリア上部：新規プロジェクト作成／作業メニュー（2026-08-19、右画面の再構成の第一弾として追加、同日中にレイアウト入替）**：サイドバー（設定項目）の右側、メインエリアの最上部は左右2カラムに分かれる（`col1`/`col2`）。以前はそれぞれ「⬆️ データアップロード」「✅ 分析設定の確認」＋その下に単一の「🚀 分析開始」ボタン、という構成だったが、右画面全体を「作業」単位のコマンドインターフェイスとして再編する設計（`C:\Users\ryu\.claude\plans\after-coder-project-file-restructure.md`に合意事項を記録）の一環として、以下のように変更した。**当初は左カラムに4つの実行ボタンをすべて縦積みしていたが、「左カラムが縦に長くなりすぎる」という指摘を受け、ボタン類を右カラムへ移し、右カラムにあった設定確認をその下の小見出しとして統合する形にレイアウトを入れ替えた**（＝ファイルアップロードだけの左カラムと、実行ボタン＋設定確認をまとめた右カラム、という現在の形）。

- **左カラム：📋 新規プロジェクト作成**（旧「⬆️ データアップロード」→旧「📋 作業メニュー」から改称）
  - **📁 ファイルアップロード**：`st.expander`（未アップロード時は自動展開、アップロード済みなら閉じた状態から始まる）の中に、従来の3方式（①テキストファイル②直接貼り付け③Excel、3節参照）の入力UIをそのまま格納。中身・挙動は変更していない。
  - 直下に「注・既存のプロジェクトファイルを使う場合は、サイドバー左下『📂 プロジェクトファイルを開く』から。」という案内文を追加（既存プロジェクトを開く導線がサイドバー側にあることが分かりにくいという想定への対応）。
- **右カラム：📋 作業メニュー**（旧「✅ 分析設定の確認」の位置。「設定確認」はこの中の小見出しとして統合）
  - **4つの実行ボタン**（上から）：
    - **📁 プロジェクトの作成のみ**（2026-08-19、同日追加）：コードブック・コーディングは一切行わず、アップロード済みの`items`と現在のサイドバー設定を、そのままコードブック無し（`codebook=None`, `codes=[]`, `coded_count=0`等）の新規履歴エントリとして保存するだけの軽量な操作。**APIキーは不要**（`project_only_ready = bool(items and q_name)`——他の3ボタンの`upload_ready`と異なりAPIキーを判定条件に含めない。AI呼び出しを一切行わないため）。ユーザーの想定用途：「コーディング等の作業無しにプロジェクトファイルだけ作成する」——列マッピング等の準備作業だけを済ませてプロジェクトファイルに保存し、後で（あるいは他の人が）続きから作業できるようにする。
    - **📐 コードブックの作成**：`run_pipeline(..., 0, ...)`——コーディング範囲の設定値に関わらず、常に`sample_size=0`（コーディングなし）で実行する。以前の「コーディング範囲」で「コーディングしない」を選んで「🚀 分析開始」を押す操作に相当（4.5節参照、この選択肢自体は削除済み）。
    - **📐➡️🧮 コードブックの作成とコーディング**：策定後、サイドバーの「🧮 コーディング範囲」で指定した件数までコーディングする。以前の「🚀 分析開始」ボタン（`sample_size≠0`の場合）と全く同じ処理（`_build_codebook_and_codes`→`_start_coding_job('initial', ...)`）。
    - **🧮 コーディング（現在のコードブックを使用）**：今アクティブなプロジェクト（`_active_result_now`、後述）に既にコードブックがある場合のみ有効化され、押すとそのコードブックのまま、サイドバーの「コーディング範囲」で指定した件数を最初からコーディングし直す（`_start_coding_job('recode', ...)`）。フォーカスするコードブックは常に「今アクティブな1つ」に限定する（ユーザー判断：「フォーカスするコードブックが複数あるのは混乱する」）。
    - 「コードブックの作成」系ボタンは、既にコードブックがある状態（プロジェクトファイルを開いた直後など）で押すと、RAWデータからの作り直し＝**リビルド**になる。専用のリビルドボタンは用意せず、ボタン直上のキャプションで説明する形にした（作業メニューのボタン数を絞るための判断）。
    - **4ボタンとも`type='primary'`（目立たせたいという要望を受けて変更）**。
  - `_active_result_now`：`st.session_state.history`から`active_history_id`に一致するエントリの`result`を引く軽量な即席参照。`col1`・`col2`の両方で使うため、`col1`/`col2`に分岐する直前で1回だけ計算する。**col2の4ボタンはいずれも成功後に`st.rerun()`するため、`_active_result_now`は常に「ボタンを押す前」の状態を見ている**（本来の`active_result`計算＝結果表示に使う方は、ボタン処理の"後"に改めて行う）。
  - **不具合と修正（2026-08-19、実機テストで発見）**：「📐 コードブックの作成」ボタンは元々`st.rerun()`を呼ばない実装だった（旧・策定のみパスを踏襲）。このため、コードブック作成が成功しても、**同じスクリプト実行内で先に評価済みの`coding_ready`（＝「🧮 コーディング」ボタンの有効/無効判定）が古い`_active_result_now`のまま**になり、画面にはコードブックが表示されているのに「🧮 コーディング」ボタンだけ無効のまま、という食い違いが発生していた。「📐 コードブックの作成」の成功時にも`st.rerun()`を呼ぶよう修正し、次のスクリプト実行で`_active_result_now`／`coding_ready`／進捗バッジがすべて新しいコードブックを反映するようにした。
  - **##### ✅ 設定確認**（4ボタンの下に続く小見出し。以前は独立した右カラムだったが、レイアウト入替により作業メニューの一部に統合）
    - **進捗バッジ**：「プロジェクト登録」「コードブック」「コーディング結果」の3項目を✅/⬜で表示する。判定は`has_project = bool(items) or _active_result_now is not None`／`has_codebook = bool(_active_result_now and _active_result_now.get('codebook'))`／`has_coding = bool(_active_result_now and _active_result_now.get('coded_count', 0) > 0)`。
    - **「設定状況」（同日、常時表示に変更）**：以前は`items`/`q_name`/`api_key`が揃うまで案内文だけを表示していたが、**サイドバーの設定内容は常に確認できた方がよい**という指摘を受け、条件分岐をやめて「設定状況」という小見出しの下に、プロジェクト名・コード数上限・策定方式・コーディングモデル・リスクチェック・APIキー状態を常時表示するようにした（アップロード前でも見える）。回答数・処理時間目安の2項目だけは`items`（アップロード結果）に依存するため、`items`が存在する場合のみ追加表示する。

**固定タイトルの削除とタブの上詰め（同日）**：ログイン後に常時表示されていた固定見出し（「👻 アフターコーディング支援ツール」＋サブタイトル）を削除した（ブランディングは既にサイドバーに統合済みで重複していたため）。あわせて`div[data-testid="stMainBlockContainer"] { padding-top: 2rem !important; }`をスタイルに追加し、既定の上部余白を縮小した。Streamlit自体のヘッダー（右上のDeployボタン等）は今回は非表示にしていない（さらに面積が必要になれば次の対象）。

**タブ構造（2026-08-16追加、2026-08-19に大幅改訂・段階的に5タブ化）**：分析結果の表示エリア全体は`st.tabs(['🏠 ホーム', '📋 基本集計表', '🔧 コードブック編集', '📊 クロス集計', '🫧 バブル図'])`の5タブに分かれる（3つ目以降は同日中に新設。3つ目=4.10節、4つ目=5.4.3節、5つ目=5.4.4節）。**当初はプロジェクトが無い間タブ自体が存在せず、作業メニュー相当のUIがタブの外（上）に常設されていたが、「タブを画面最上部に固定したい」という要望を受け、`st.tabs()`をスクリプトの最初（`if active_result:`より前）で無条件に呼ぶよう変更した**。これに伴い、作業メニュー・設定確認（本節冒頭）を含む**画面のほぼ全内容が`🏠 ホーム`タブの中に移動**した：
- `🏠 ホーム`タブの内側の並び順（上から）：①📖使い方・プロジェクトファイルについて（折りたたみ）②新規プロジェクト作成／作業メニュー（`col1`/`col2`）③（ボタン処理後に改めて`active_result`を判定）④APIコスト⑤完了メッセージ⑥コードブック表示（表示専用、無ければ案内のみ）⑦コーディング結果セクション（本節後半）。**編集案プレビュー・精度診断・自由チャット編集は含まれない**（2026-08-19、すべて`🔧 コードブック編集`タブへ移設。4.10節）。
- **コードブック未作成のプロジェクトを開いている場合（`result.get('codebook')`が`None`）**：⑥をスキップし、「このプロジェクトはまだコードブックがありません。作業メニューの『📐 コードブックの作成』または『📐➡️🧮 コードブックの作成とコーディング』を実行してください。」という案内のみ表示する。「📁 プロジェクトの作成のみ」ボタン（本節前半）で作られたプロジェクトを開いた直後がこの状態にあたる。⑦のコーディング結果セクションは元々`coded_count > 0`でガードされているため、この場合は追加の対応なしで自然にスキップされる。
- `📋 基本集計表`タブは、`active_result`が無い場合は「コーディング結果がまだありません。〜作業メニューで...」という案内のみを表示し、ある場合のみ`_render_basic_table_tab(result)`を呼ぶ（5.4.1節・5.4.2節）。
- `🔧 コードブック編集`タブ（2026-08-19新設）は、`active_result`が無い場合は他の2タブと同様の案内のみを表示し、ある場合のみ`_render_codebook_edit_tab(result)`を呼ぶ（4.10節）。
- `📊 クロス集計`タブ（2026-08-18新設）・`🫧 バブル図`タブ（2026-08-19新設）も同様に、`active_result`の有無で案内文と`_render_cross_tab_tab(result)`／`_render_bubble_tab(result)`の呼び分けを行う（5.4.3節・5.4.4節）。
- **5タブとも、プロジェクトの有無に関わらず常に選択できる**（以前は`active_result`が無いと`📋 基本集計表`タブそのものが存在しなかった）。
- `result`/`q_name`/`coded_count`/`total_items`は`🏠 ホーム`タブの内部（`col1`/`col2`のボタン処理より後）で算出され、他のタブ側でも同じ変数（Pythonの`with`/`if`はスコープを作らないため、モジュールレベルではタブをまたいでそのまま参照できる）を使う。

#### 5.4.3 クロス集計タブ（`_render_cross_tab_tab`、2026-08-18新設・同日〜19日で改善）

Excel取り込み時に指定した属性列（年代・職位など）を1つ選び、コード×属性値のクロス集計表（実数・出現率%）と横棒グラフを表示する。

- 属性が1つも取り込まれていない場合（`items`に`attrs`が無い）は案内のみ表示して終了する。
- 属性を選択→「📊 クロス集計を開始」ボタンで`st.session_state.cross_tab_active_attr`に確定し、以降はそれを使って再計算する（ボタンを押し直すまで選択を変えても再計算しない、明示的な実行ステップ）。
- **集計表**：カテゴリー・コード×属性値ごとに「実数」「％」の列を持つ。％は各属性値グループの有効回答数（n）に対する出現率（既存のGT集計と同じ考え方）。表の先頭2行に**n（基数・人数）**と**グロス(g)（延べタグ数、複数コード該当時はnより大きくなる）**のサマリー行を追加（ユーザー要望：「n数とグロスをどこかに表示して」）。
  - **カテゴリー列の背景色**：ホームの「コード別GT集計」グラフの棒と同じ配色（`_category_color_map`）をフル濃度・白文字で適用（`pandas.DataFrame.style.apply`→`st.dataframe`。ユーザー要望：「色分けはコード欄ではなくカテゴリー欄に」）。n/グロスの行は薄いグレーで別枠と分かるようにする。
- **グラフ**：Plotlyの横棒グラフ（`orientation='h'`）、属性値ごとに色分けしたグループ棒。コード数が多くても読めるよう、コード数に応じて高さが伸びる縦長レイアウト。
  - **y軸ラベルの色分け**：コード名の先頭に、そのコードが属するカテゴリーの色の█（大きめのブロック文字。当初は■だったが視認性向上のためユーザー要望で変更）を付ける。Plotlyの`ticktext`に`<span style="color:#...">█</span>`というHTML断片を渡すと実際に色が反映されることを、使い捨てのStreamlitアプリでSVGのfill属性を直接確認して検証済み（2026-08-19）。
  - **カテゴリー凡例**：グラフ下に中央揃えで「█ カテゴリ1　█ カテゴリ2　…」という凡例を表示（グラフの色チャンネル自体は属性値の比較に使っているため、カテゴリー色は別途この凡例で示す。ユーザー要望）。凡例の文字サイズは、y軸のコード名ラベルと同じ12pxに明示的に固定している（`TICK_FONT_SIZE`という1つの変数を両方に使うことで、Streamlitのテーマが変わっても常に一致する。ユーザー要望：「凡例の文字サイズをグラフ内のコード名のサイズと同じに」）。
- 表・グラフとも、並び順は順A（カテゴリ出現率順→コード出現率順）でホーム・基本集計表と統一。

#### 5.4.4 バブル図タブ（`_render_bubble_tab`／`_build_bubble_svg`、2026-08-19新設）

大画面・ポスターでの提示を想定した、放射状のバブル図（中央から見て カテゴリー→コード→代表引用文 の順に外側へ広がる）を生成する機能。ユーザー要望の規模感は「最大10カテゴリー×40コード」。

- **UI**：対象カテゴリーの`st.multiselect`（既定で全選択）→「🫧 バブル図を生成」ボタン。カテゴリー数・コード数が多すぎて見づらい場合、ユーザー自身が対象カテゴリーを絞って複数回生成することで「2枚に分ける」運用ができる（自動分割ロジックは持たず、選択の自由度で対応する設計）。
- **レイアウトアルゴリズム（`_build_bubble_svg`）**：2000×2000のSVGキャンバス上に、①カテゴリーを大きな円周上（半径700）に均等配置し、②各カテゴリーの周りにそのカテゴリーのコードを扇状（コード数に応じて開き角・半径を調整）に配置してカテゴリー中心から細い線で結び、③各コードバブルの外側に、該当する代表的な原文（`_representative_quotes`、最大3件、1回答につき1文まで、20文字で切り詰め）を小さな角丸ラベルとして添える。
- **色**：カテゴリーバブルはホームの「コード別GT集計」グラフと同じ`_category_color_map`のフル濃度色、コードバブルは同じ色の淡色版（`_lighten_hex`、Excel出力の`cat_color_pale`と共通の関数）——同じカテゴリーの親子関係が色でも分かるようにしている。
- **バブルの大きさ**：該当数（GT件数）に応じた大中小3段階（`_bubble_size_tier`：上位1/3=大、中間1/3=中、それ以外=小）。完全な比例ではなく大まかな差で十分というユーザー要望に対応（「完全比例ではありませんが…大中小（差は大きくなくてよい）」）。
- **出力形式**：SVG（ベクター）で生成する。ラスター画像と異なり拡大しても文字が滲まないため、ポスター印刷や大画面提示に向く（ユーザー要望：「大画面やポスターで見せることを想定しているため、画面上は文字が小さくなっても構いません（拡大すると読めるといい）」）。画面上は900px枠内にスクロール表示し、ダウンロードボタンでSVGファイル一式（実寸2000×2000）を取得できる。
- **`st.html`ではなく`st.iframe`を使う理由（実機で発見した不具合）**：`st.html`はDOMPurifyでサニタイズされ、既定では`<svg>`要素がまるごと除去されてしまうため、生成したSVGが一切表示されなかった。生のHTML文字列をそのまま埋め込める`st.iframe`に切り替えて解決した——このタブのコンテンツは外部/ユーザー入力ではなく自分で生成したSVGのみなので、`st.iframe`のraw HTML経路（サニタイズなし）を使うリスクは無い（引用文のテキストは`_xml_escape`で別途エスケープ済み）。
- **もう1つの実機不具合（SVGの高さ0collapse）**：生成したSVGに`width`/`height`属性が無く`viewBox`のみだった場合、高さ未確定の親要素（`overflow:auto`のdiv）の中では既定の「100%」が0に解決され、何も表示されなくなった。`<svg>`タグに`width="2000" height="2000"`を明示することで解決。
- **調整可能パラメータ（2026-08-19、同日中に追加）**：「⚙️ 詳細設定」の折りたたみに、カテゴリー・コード・文（代表引用文）それぞれについて4項目（親バブル中心からの距離／バブルサイズ倍率／文字サイズ／文字数）、計12個のスライダーを用意（ユーザー要望：「カテゴリー、コード、文ごとに、バブルサイズ、バブルの中心（上位バブルの中心）からの距離、文字サイズ、文字数」）。
  - `_build_bubble_svg(..., params=None)`が`BUBBLE_PARAM_DEFAULTS`（初期実装時の固定値と同じ既定値）とマージして使う。`params`省略時は従来と完全に同じ見た目になる。
  - 「距離」は基準値として扱う：コードの距離（`code_dist`）にはコード数に応じた密集回避の自動調整（`+ min(90, n_codes*6)`）が、文の距離（`quote_dist`）には引用文の段積みオフセット（`k * (quote_dist*1.2+8)`）がそれぞれ加算された上乗せされる。ユーザーがスライダーで動かすのは「土台」で、自動調整自体は無効化しない設計（密集で完全に潰れることを防ぐため）。
  - 「バブルサイズ倍率」は大中小のサイズ階層（`_bubble_size_tier`）の基準値に掛ける倍率。大中小の相対的な差は維持したまま全体を拡大縮小する。
  - 「バブル図を生成」ボタンを押した時点のスライダー値を`st.session_state.bubble_svg_params`に確定する（対象カテゴリーの確定と同じパターン）——スライダーを動かしただけでは再生成されず、明示的な実行ステップを踏む。
  - 検証：合成データ（10カテゴリー×40コード）で、`cat_dist`変更が実際にカテゴリー円のy座標に反映されること（差分が指定通りぴったり一致すること）、`cat_size`変更でカテゴリー円の半径が実際に拡大されること、`code_font`/`code_chars`変更がSVGのfont-size属性・折り返しに反映されることを単体テストで確認。
- **見た目の調整（2026-08-19、実機での見た目確認を受けて同日中に追加修正）**：
  - **タイトル**：中央揃え・太字・大サイズから、左上・レギュラー体・元の2/3サイズ（34→23px）に変更（ユーザー要望：「タイトルはタイトル文字だけを左上に。太さはレギュラーでサイズは現在の2/3程度」）。
  - **カテゴリー文字サイズの自動大中小化**：`_CAT_FONT_TIER_MULT = {'large': 1.25, 'medium': 1.0, 'small': 0.8}`を`cat_font`（スライダーの値＝中サイズの基準値）に掛け、カテゴリーバブルのサイズ階層と連動した文字サイズを自動的に割り当てる（ユーザー要望：「カテゴリーの文字サイズはバブルサイズに合わせて最初から大中小になっていてほしい」）。スライダー自体は「中」の基準値を動かす役割として残る。
  - **引用文ラベルの枠を廃止**：白い角丸`<rect>`＋枠線を削除し、テキストのみを表示するようにした（ユーザー要望：「ラベルの枠は不要」）。枠が無くなったため「ラベル幅（`quote_size`）」パラメータ・スライダーも削除した（枠の幅を指定する意味が無くなったため。「ラベル幅の調整は不要」）。
  - **引用文の重なり軽減（ユーザー相談「ラベルの重なりだけが調整できない…斜めとか、放射状などにするとか」への対応）**：2つの工夫を組み合わせた。①同じコードの複数の引用文を、コードの向きを中心に26度の扇状（`fan_spread_deg`）に振って配置する（1本の直線上に並べるより隣の文とぶつかりにくい）。②カテゴリー内で隣り合うコード（`j`が偶数/奇数）ごとに引用文までの距離を互い違いにずらす（`stagger = (j % 2) * (quote_dist * 1.4)`）ことで、隣接コードの引用文が同じ「輪」に重なって並ぶのを避ける。**完全な衝突回避（テキストの実サイズを考慮した反発計算など）ではなく、経験則ベースの緩和策であることは明示しておく**——コード数・引用文の件数によっては、まだ重なりが残るケースがありうる。
  - **カテゴリー・コード名の折り返しを2行→3行に拡張（同日、確認前の追加要望）**：`_svg_text_block(..., max_lines=2)`だった呼び出し2箇所（カテゴリー名・コード名）を`max_lines=3`に変更。長い名称でも省略記号（…）に頼らず表示できる範囲が広がる。`_svg_text_block`関数自体の既定値（`max_lines=2`）とタイトル・引用文の呼び出し（1行のまま）は変更していない。
- **実データでの表示確認を受けた追加改善（2026-08-19、同日さらに追加）**：ユーザーが実際のプロジェクトでスクリーンショットを共有し、「文の重なりがもう少しなんとかなると嬉しい。個別調整などでもよい」「画面の表示スケールが変えられないか」の2点を要望。
  - **引用文の重なり軽減（v2）**：v1（26度固定の扇＋2段階の距離ずらし）では、コード数が多いカテゴリーや引用文が長い場合に重なりが残っていた。以下に強化：
    - 扇の開き角を固定値ではなく、最長の引用文の推定文字幅（`文字数 × quote_font × 0.95`）とその引用文が置かれる半径から動的に計算する（`min_fan_deg`、26〜80度の範囲）。長い引用文ほど扇を広げ、必要な横幅を確保する。
    - 隣接コード間の距離ずらしを2段階（`j % 2`）から3段階（`j % 3`）に増やし、密集したカテゴリーでもコードごとに異なる「輪」に配置されやすくした。
    - 同じコード内の引用文どうしの半径方向の間隔も`quote_dist * 0.9`→`quote_dist * 1.3`に拡大。
    - **「表示件数の上限（コードごと）」スライダーを新設**（0〜3、既定3）：ユーザー提案の「個別調整」に対応する、最も簡単で確実な手段として、コードごとに表示する引用文の件数自体を減らせるようにした（`_representative_quotes`の`max_quotes`に連動）。0にすると引用文を一切表示しない。
    - **正直な限界**：これは経験則ベースの緩和策の強化であり、テキストの実際のレンダリング幅を厳密に測ってから配置を決める衝突回避（力学シミュレーション等）ではない。データの分布次第では、まだ重なりが残るケースがありうる——その場合は「表示件数の上限」を減らすか、「文字数（切り詰め）」を短くするのが実用的な回避策になる。
  - **画面表示スケールの調整**：バブル図の下（生成結果表示の直前）に「画面表示の倍率(%)」スライダー（20〜150%、既定45%）を新設。SVGは`viewBox="0 0 2000 2000"`を保ったまま、画面表示用のコピーだけ`width`/`height`属性を`round(2000 × 倍率/100)`に置換する（`svg.replace(...)`による単純な文字列置換、`_build_bubble_svg`本体は変更しない）。ベクターなので画質劣化なく拡大縮小できる。**ダウンロードボタンは元の`svg`（実寸2000×2000、置換前）を使い続けるため、印刷・ポスター用の解像度には一切影響しない**。既定45%は、900pxの表示枠に図全体がおおよそ収まる大きさ（2000px×45%≒900px）を意図している。`BUBBLE_CANVAS_SIZE = 2000`という共有定数を新設し、`_build_bubble_svg`内のキャンバスサイズと画面表示スケール計算の両方から参照する（値の重複を避けるため）。
  - 検証：`quote_max_count=0`で引用文が生成されないこと、`quote_max_count=1`でtext要素数が期待通りになること、width/height置換ロジックが`viewBox`を変えずに`width`/`height`だけを書き換えることを単体テストで確認。画面表示スケールは使い捨てのStreamlitアプリ＋ブラウザで、スライダー操作によって実際にiframe内のSVGサイズが変化することを確認（45%→145%でtitleが画面いっぱいに拡大表示された）。クリーン起動でエラー無し。引用文重なり軽減v2の実データでの効果は未確認——実際のプロジェクトでの確認を推奨。
- **検証**：`_next_code_id`類似の採番はここでは使わない（新規コードを作らないため）。構文チェック、`_build_bubble_svg`の単体テスト（ユーザー想定規模＝10カテゴリー×40コードで合成データを作り、XMLとして妥当にパースできること、カテゴリー/コードのcircle要素数・接続線の本数・text要素数が期待通りであること、サイズ階層が複数種類生成されること、カテゴリー文字サイズが複数種類生成されること、タイトルの位置・太さ・サイズ、1カテゴリーのみ選択した縮退ケースでも壊れないこと）、`_bubble_size_tier`/`_svg_wrap_lines`/`_representative_quotes`の単体テスト、使い捨てのStreamlitアプリ＋ブラウザでの実描画確認（DOM上のcircle/rect数、色のfill値、st.iframe内での実表示）、クリーン起動。引用文の重なり軽減策は実データでの見た目を未確認——実際のプロジェクトでの確認を推奨。

`coded_count > 0` の場合のみ「📊 コーディング結果」セクションが表示される（4.5節）。

表示順は上から次の通り（カテゴリ別集計を先に、グラフは最後に表示する。グラフの並び替えラジオに関係なく確認できるようにするため）：

- **「📌 特記情報」折りたたみボックス**（`st.expander`、既定で展開表示）：センチメント（ポジティブ／ネガティブ／ニュートラル／**混在**の4区分、5.2節参照）、非該当、回答分類（不明・無回答、4.5.4節、常に表示）、有効化されたリスクチェック項目（4.5.3節）の該当件数・割合を**1行にまとめて表示**する（`st.columns`ではなくflexboxのdiv1行にまとめ、ブラウザ幅が狭くてもStreamlitの自動縦積みが起きないようにしている）。旧・「😊 センチメント集計」という独立した見出しは廃止し、この折りたたみボックスに統合した。表示順は「ポジティブ→ネガティブ→ニュートラル→混在→非該当→不明→無回答→（有効化されたリスクチェック項目、`RISK_CHECK_OPTIONS`の順＝クレーム→個人情報→団体情報→危険情報）」。
- カテゴリ別集計表（折りたたみ）
- コード別GT集計の縦棒グラフ（カテゴリ別色分け）。並び順は「順A：カテゴリ出現率順→コード出現率順」「順B：コード出現率が多い順」から選択可
  - **配色**：`_category_color_map(gt, codes)`（モジュール直下の共通関数）が、カテゴリの出現件数合計が多い順に固定配色`CHART_COLORS`（10色）を割り当て、`px.bar(..., color_discrete_map=...)`で明示的に指定する。以前は`color='カテゴリ'`のみでPlotly Express（実際にはStreamlit側が自動適用するテーマ配色）に任せていたため、ブラウザ／OSのライト・ダーク設定や再実行のたびに同じカテゴリでも色が入れ替わることがあった。`color_discrete_map`を固定することで、Excelレポート（5.5.1節）と画面表示の色を常に一致させ、かつ画面自体の色の揺れも解消している（2026-08-03、ユーザーからの「Excelと画面の色が同系色で濃淡が逆転している」という指摘を受けて修正）。
  - **`color_discrete_map`のキーはgtのcat_name由来**：`color_discrete_map`のキー（カテゴリ名の文字列）は、`df_plot`（グラフの実データ）と必ず同じソース＝`gt`から作る（`_cat_name_map = {g['cat_id']: g['cat_name'] for g in gt}`）。以前は`result['codes']`（＝現在のコードブック）から作っていたため、コードブック編集でカテゴリ名を変更した後（`gt`は編集前のコーディング実行時点の名前のまま）に名前が食い違い、Plotlyが該当カテゴリの色をマップ内に見つけられず自動配色にフォールバックしてしまう不具合があった（2026-08-05、ユーザーから「グラフカラーの乱れが以前からたまにある」と報告を受け発見・修正。カテゴリ名を変更した直後、その分析を再コーディングせずにグラフを見た場合にのみ再現する、という発生条件だったため「たまに」としか認識されていなかった）。Excel側のグラフ（5.5.3節）は色をカテゴリID経由の系列順で割り当てており、文字列一致に依存しないためこの不具合の対象外。
  - **順Aソートは`cat_total`の生値ではなく`cat_rank`（順位）で並べる**：`gt_sorted`（画面グラフの表示順）の並び替えキーは、`_category_color_map`が確定させた`cat_order`から`cat_rank = {cid: i for i, cid in enumerate(cat_order)}`を作り、`(cat_rank, -count)`でソートする。以前は`cat_total`（カテゴリごとの出現数合計の生値）を直接キーにしていたため、**2つのカテゴリの合計が同数でタイになると**、タイの間だけコード件数で全体を横断比較してしまい、同じカテゴリのコード同士が離れた位置に分断されてしまう不具合があった（色分けが「乱れて」見える原因。2026-08-10、ユーザー提供のExcelレポートで`CAT03`と`CAT02`の合計がともに25件でタイになっており、これが原因と特定）。`codes_sorted_a`（5.5.1節）・`gt_sorted_a`（5.5.3節）は元から`cat_rank`ベースで実装されていたため、Excel側は最初からこの不具合の対象外だった。ランク（整数の順位）はタイになり得ないため、この方式なら同じカテゴリのコードは常に連続する。

#### 5.4.1 基本集計表タブ（`_render_basic_table_tab`、ドリルダウン形式、2026-08-16追加）

ユーザー提供のレイアウト見本（`開発指示用素材\基本集計表.xlsx`）に基づく、GT集計から実際の回答文へと段階的に絞り込んでいく探索用の画面。目的はドリルダウン（カテゴリー→コード→該当文→[部分一致の場合のみ]原文）であり、一覧性より「気になったコードの中身を素早く覗ける」ことを優先している。`coded_count == 0`の場合は「🏠 ホームタブでコーディングを実行してください」という案内のみ表示する。

- **左カラム：GT集計（コード選択）**：`_category_color_map`が確定させる順A（カテゴリ出現率順→コード出現率順、5.4節のグラフと同じ並び）でカテゴリ→コードを列挙し、コードごとに`st.checkbox`（コード名・出現率・出現数を表示）を置く。カテゴリー見出しの接頭には、そのカテゴリの色（`cat_color_full`、グラフ・Excelと共通のパレット）で塗った`■■`を付ける（2026-08-17追加。無色の見出しだとグラフ・Excel側の色分けとの対応が分かりにくいという指摘への対応）。
- **選択は最大2コード（比較用）**：3つ目のコードを選ぶと、最も古く選んだコードのチェックが自動的に外れる（`on_change`コールバック＋`st.session_state['basic_code_order']`という選択順リストで実装。Streamlitの`st.data_editor`のチェックボックス列では選択数の上限を直接は制御できないため、個別の`st.checkbox`＋コールバック方式にした）。
- **右下：コード該当文リスト**：選択中のコードそれぞれについて、`results`の`sentences`（5.1.1節）からそのコードが付与された文を列挙する。
  - **同一原文内に同じコードの文が2件以上ある場合は、該当する文だけを「／」で連結して1行にまとめて表示する（2026-08-17追加、同日中に方式変更）**：以前は文単位でそのまま列挙していたため、同じ回答者の発言が別々の行に分断されて表示され、一見すると別々の回答のように見えてしまうという指摘を受けた。1つの回答（`res['id']`）内で同じコードが付いた文（`matched`）が2件以上ある場合、個別の文ではなく1行にまとめる必要がある点は変わらないが、まとめ方は**原文全体ではなく、該当する文だけを「／」で連結した抜粋**にしている（5.5.4節の自由回答一覧`_excerpt_for_code`と同じ方針）。当初は`item['text']`（原文全体）をそのまま使う実装にしていたが、「原文表示率が多くなりすぎた」というユーザー指摘を受け、無関係な文まで含む原文全体ではなく該当文だけの抜粋に変更した。連結抜粋は原文と文字列として完全一致しないため、通常「✓欄は原文と一致する場合は表示しない」ルールの対象外となり✓欄が表示される（＝原文全体で見たい場合は選択できる）。回答原文パネルでのハイライトも、連結抜粋をそのまま`in`検索するのではなく、「／」で分割して各文を個別に原文内で検索・強調するようにしている。該当コードの文が1件だけの回答は、従来通りその文だけを表示し、回答が複数文に分かれていれば✓欄を付ける。
- **2コード選択時は、それぞれ独立した固定高さ・スクロール可能な窓に分ける（2026-08-17変更）**：`st.container(height=340, border=True, gap=None)`を選択コードごとに1つ作る（以前は`---`区切り線を挟んで同じスクロール領域に並べていたが、片方の該当文が多いともう片方が下に押し流されて比較しにくいという指摘を受け、`height`指定でそれぞれ独立スクロールにした）。コード見出しは各窓の**外側**（左上）に置き、窓内をスクロールしても見出しは動かない（当初は窓内の先頭要素として配置していたが、それだと窓をスクロールすると見出しごと流れてしまうという指摘を受け、窓の外に出した）。配色も当初の「カテゴリーカラー地に白文字のバッジ」から、GT集計のカテゴリー見出しと統一した「カテゴリーカラーで塗った`■■`を接頭」に変更（`cat_color_full`を再利用）。
- **✓欄は「回答が複数文に分かれている場合」だけ表示する**：その文が回答全文と完全に一致する場合（＝その回答が1文しかない場合）は、原文を別途表示する意味が無いため、✓欄自体を出さない（ユーザー要望：「コード該当文が原文と一致する場合はチェックボックスが表示されない処理が最もよい」）。複数文に分かれている回答の文にだけ✓欄が付く。
  - **✓欄の有無に関わらず、常に同じ2カラム構成（`st.columns([0.06, 0.94])`）でインデントを揃える（2026-08-17追加修正）**：以前は✓欄が無い行（全文一致の文）だけ`st.columns`を使わず直接書いていたため左端に寄って見えていた。`_render_sentence_row()`という共通の描画関数に統一し、✓欄が不要な行は左カラムに何も置かず空白のまま残すことで、✓欄がある行・無い行で本文の開始位置が揃うようにした。
  - **本文と属性は1つの`st.markdown`呼び出しにまとめ、行間を詰める（2026-08-17追加修正）**：以前は`st.write(本文)`＋`st.caption(属性)`という2つの独立した要素に分けていたため、Streamlit側の既定の要素間マージンが積み重なり縦に間延びして見えていた。本文と属性（`font-size:0.78em`の薄いグレー）を`"本文  \n属性"`という1つのMarkdown文字列にまとめ、さらに窓自体を`st.container(..., gap=None)`にすることで、段落間・行間を詰めている。属性行の先頭には全角スペース2つ（`　　`）を入れて本文から2文字分インデントする（半角スペースはHTML描画時に連続すると詰められてしまうため、崩れない全角スペースを使用）。
- **文選択は常に1件のみ**：✓欄がある文をチェックすると、別の文の✓が自動的に外れる（`st.session_state['basic_selected_sentence']`で管理）。目的はドリルダウンであり、複数文を並べて比較する用途ではないため。
- **右上：回答原文**：文を選択すると、その文の元になった回答の全文（`item['text']`、`_split_sentences`で分割する前の生データそのもの）を表示し、選択した文の該当箇所だけを`**:orange[...]**`（Streamlitのcolor付きMarkdown記法）で強調する。何も選択していない間は案内文のみ表示する。

2026-08-16、上記のロジック（順Aソート、コード選択の2件上限＋最古自動解除、単一文選択、単一文しかない回答へのチェックボックス非表示、選択文の原文内ハイライト）を、Streamlitの`st.checkbox`等をモックした標準スクリプトで7件のアサーションにより検証済み。2026-08-17、細部の見た目調整（■■配色・独立スクロール窓・コード見出しバッジ・インデント統一・行間短縮）を追加の標準スクリプト（6件のアサーション：カテゴリー見出しの配色、選択コードごとの`height`付きコンテナ、コード見出しバッジの配色、属性行が本文と同じmarkdown要素に統合されていること、全文一致の行も2カラム構成でインデントが揃うこと）で検証。同日中に、コード見出しを窓内バッジから窓外の`■■`接頭表示へ変更（`ast.parse`による構文チェックとクリーンなサーバー起動でエラーが無いことを確認）。さらに同日、本番環境（Streamlit Community Cloud）での実データテストで「同一原文内の2文が同じコードで別々の行に分断されて見える」という指摘を受け、同一原文・同一コードの文が2件以上ある場合に原文全体を1行へまとめる変更を追加し、標準スクリプトで4件のアサーション（2文同一コードの原文が1行にまとまり分断されないこと、そのマージ行に✓欄が出ないこと、単一文一致行に✓欄が出ないこと、部分一致行には✓欄が出ること）により検証。その後さらに同日、「原文表示率が多くなりすぎた」という指摘を受け、原文全体をまとめる方式から、該当する文だけを「／」で連結する方式（自由回答一覧と同じ方針）に変更し、標準スクリプトで5件のアサーション（連結抜粋が「／」区切りで出力され原文全体ではないこと、連結抜粋行には✓欄が出ること、単一文一致・部分一致それぞれの従来動作が維持されていること、連結抜粋選択時に回答原文パネルで各文が個別にハイライトされること）により再検証。実際のブラウザ操作によるE2E確認は、ログインゲート・APIキーが必要なため未実施。

#### 5.4.2 統合候補・分割候補（基本集計表タブ内、2026-08-19追加）＋ 🔧コードブック編集タブ（新設）

基本集計表タブでコードを眺めながら「このコードとこのコードは統合すべきでは」「このコードは細かすぎるので分割すべきでは」と気づいた際に、その場で候補として一時登録し、後で新設の`🔧 コードブック編集`タブへまとめて送れる機能。**実際にLLMが統合・分割を提案する処理（コードブック編集タブの本体機能）は未実装**——今回はあくまで「候補を集める」ところまで（今後の設計課題として別途詳細化する）。

- **候補の一時登録**：基本集計表のコード該当文リスト（本節前半）の下に、「🔗 統合候補」「✂️ 分割候補」の2ボタンを新設。
  - 「🔗 統合候補」：GT集計で**コードを2つ選択している場合のみ**有効。押すと、選択中の2コード（`code_ids`・`code_names`）を`result['merge_candidates']`（リスト）に追加する。
  - 「✂️ 分割候補」：GT集計で**コードを1つだけ選択している場合のみ**有効。押すと、選択中の1コードを`result['split_candidates']`に追加する。
  - **重複登録防止（`_claimed_code_ids()`、2026-08-19に強化）**：1つのコードは、統合・分割を問わず**同時に1つの候補にしか使えない**。統合候補・分割候補の両リストに登録済みの全コードIDを集合として集め（`_claimed_code_ids()`）、選択中の組み合わせに含まれるコードが1つでもこの集合に含まれていれば、統合候補・分割候補どちらのボタンも無効化する。
    - **経緯**：当初は「選択中の2コードの組み合わせが、統合候補リストに全く同じ組み合わせとして既に存在するか」だけをチェックしていた（`set(ids)`の完全一致判定）。実機テストで「片方のコードだけが別の組み合わせで既に登録済みの場合、重複登録できてしまう」と報告を受け、判定を「コード単位」に強化した。同じコードに対して矛盾する編集案（例：統合と分割の両方）が並行して作られないようにする狙いもある（ユーザー方針：「集合的に捉えて現在のコーディング結果の総件数に変化がないように」）。
    - **これは「今リストに残っている間だけ」の判定**——「解除」で取り除くか、「コードブック編集タブに送る」で送信済みになれば、そのコードは再び統合・分割候補として使えるようになる（送信履歴を恒久的に記憶するわけではない、ユーザー確認済みの仕様）。
  - 候補は`result`（プロジェクトごと）に保持し、変更のたびに`st.session_state.history`の該当エントリへ書き戻す（他の編集系機能と同じ`_save_basic_table_result()`パターン）。
- **候補リストの表示**：「🔗 統合候補リスト」「✂️ 分割候補リスト」を基本集計表の下部に表示。各項目に「解除」ボタン（そのエントリだけをリストから削除）。
- **「コードブック編集タブに送る」ボタン**：各リストの下に1つずつ配置。押すと、そのリストの**全項目**を`result['edit_queue_merge']`／`result['edit_queue_split']`（コードブック編集タブ側が読むキュー、蓄積型でリストを空にしない限り増え続ける）へ転記し、送信元の基本集計表側のリスト（`merge_candidates`／`split_candidates`）は空にする。
- **`🔧 コードブック編集`タブ（新設）**：`_render_codebook_edit_tab(result)`。`st.tabs()`が3タブ構成になった（`🏠 ホーム`／`📋 基本集計表`／`🔧 コードブック編集`）。現時点では`edit_queue_merge`／`edit_queue_split`の中身を単純に一覧表示するだけの「器」——統合・分割の実行（LLM呼び出し・確認UIなど）は未実装。両キューが空の場合は「基本集計表タブの『🔗 統合候補』『✂️ 分割候補』から送られたコードがここに表示されます」という案内のみ表示する。`active_result`が無い場合（プロジェクト未作成）は、他の2タブと同様に案内メッセージのみ。
- 2026-08-19、標準スクリプトで9件のアサーション（統合候補の登録・重複防止・分割候補の登録・解除・コードブック編集タブへの送信とリストのクリア・送信後にコードブック編集タブ側で内容が表示されること、および3タブ構成になっていることの静的検証）により検証。同日、実機テストで見つかった「片方一致の重複登録」不具合の修正後、`_claimed_code_ids()`のコード単位判定を追加の標準スクリプトで6件のアサーション（片方一致の組み合わせがブロックされること、未使用コードは登録できること、統合候補で使用済みのコードは分割候補にも使えないこと、解除後は再び使えるようになること）により再検証。クリーンなサーバー起動でエラー無しを確認。

### 5.5 Excelレポート（`create_excel`）

`coded_count > 0` の場合にダウンロードボタンが表示される。未コーディング分がある場合は見出しに「（N/全件数件分）」と付き、**コーディング済みの範囲のみ**でレポートが作成される（全件データではない点に注意）。

4シート構成：
1. **集計レポート**：特記情報集計（センチメント＋非該当＋回答分類＋リスクチェック）、コード別GT集計（転置レイアウト、カテゴリ別色分け、出現率20%以上/2%以下を強調表示）、回答別コーディング結果（RAWデータに各コードのフラグを付与した一覧）
2. **集計ダイジェスト**：同様の集計を、より簡潔な行方向レイアウトで再掲
3. **自由回答一覧**：カテゴリー・コードごとに実際の回答文を並べた一覧（詳細は5.5.4節）
4. **文単位データ**：1行1文のRAWデータ（詳細は5.5.5節）

**列配置の全体設計（2026-08-04、ユーザー提供のレイアウト見本に合わせて再設計）**：「集計レポート」シートの3ブロック（特記情報集計・コード別GT集計・回答別コーディング結果）は、共通の`FLAG_COLUMNS`（非該当＋不明＋無回答＋有効化されたリスクチェック項目、1列ずつの一文字フラグ）を軸に列位置を揃える：
- `FLAG_COLUMNS`は列1から順に並べる（例：非,不,無,ク,個,団,危のように全項目有効なら7列）。
- その直後（`META_START = len(FLAG_COLUMNS) + 1`）に「センチメント」「回答ID」「回答テキスト」の3列（この順）を置く。「回答テキスト」の列＝`FIXED_N`。
- コード列は`CODE_START = FIXED_N + 1`から始まる。
- 「特記情報集計」（センチメント〜リスクチェックの集計値）は`CODE_START`列から開始する（以前は固定の列D=4から開始していたが、コード列の開始位置に揃うよう変更）。
- 「コード別GT集計」（転置表）の行見出し列（「カテゴリー出現率順位」等のラベル）は`FIXED_N`列に置く（以前は固定の列A=1だったが、「回答テキスト」列と同じ位置になるよう変更）。
- 以前は「回答ID・回答テキスト・センチメント→フラグ列→コード列」の順だったが、「フラグ列→センチメント・回答ID・回答テキスト→コード列」に反転した。
- この設計により、有効化するリスクチェック項目数が変わって`FLAG_COLUMNS`の列数が増減しても、3ブロックのコード列開始位置は常に一致し続ける。
- 「集計ダイジェスト」シートには回答別コーディング結果や転置GT表がなく（行方向のGT集計のみ）、この列位置揃えの対象外。特記情報集計は列1から開始する既存位置のまま、項目内容（不明・無回答の追加、住所情報の削除）だけを集計レポートと共通化した。

#### 5.5.1 カテゴリ別カラー（画面の棒グラフと連動）

`cat_color_full`/`cat_color_pale`の2種類の配色マップを、画面の縦棒グラフ（5.4節）と完全に同じ色になるよう構築する：

- **色の並び順**：モジュール直下の共通関数`_category_color_map(gt, codes)`が、`gt`から`cat_id`ごとの出現件数合計を求め、多い順にソートしたカテゴリ順に、モジュール定数`CHART_COLORS`（`0068C9`,`83C9FF`,`FF2B2B`,`FFABAB`,`29B09D`,`7DEFA1`,`FF8700`,`FFD16A`,`6D3FC0`,`D5DAE5`の10色）を先頭から割り当てる（`cat_order, cat_color_full = _category_color_map(gt, codes)`）。この同じ関数を画面の`px.bar()`呼び出し（5.4節）でも使い、`color_discrete_map`として明示的に渡すことで、画面とExcelの色を常に一致させている。
  - **経緯**：2026-08-01の初回実装では画面の色をPlotly標準配色と誤認しており、2026-08-03にユーザーからのスクリーンショット比較指摘で「実際はStreamlitが自動適用するテーマ配色（ブラウザ／OSのライト・ダーク設定で並び順が変わる）」と判明。さらに一致させた直後も「同系色の濃淡が逆転しているものがある」との指摘を受け、Streamlitの配色を推測で追いかけるのをやめ、`CHART_COLORS`を独自の固定配色として画面側にも`color_discrete_map`で明示指定する方式に変更した。これにより画面・Excelの色が常に一致するだけでなく、テーマ設定や再実行による画面自体の色の揺れも解消された。
- **フル彩度（`cat_color_full`）**：コード別GT集計（転置表）の「中間カテゴリID」行、および回答別コーディング結果のコード列見出し行に使う。
- **淡色版（`cat_color_pale`）**：`_lighten_hex()`（各RGB成分を白へ65%近づける）で生成し、GT集計の他の行、回答別コーディング結果のコードフラグセル、集計ダイジェストのGT集計行に使う。
- 出現率が20%以上／2%以下のセルは、従来通り`HIGH_FILL`/`LOW_FILL`（緑／赤系の強調色）で上書きされる（カテゴリ色より優先）。
- **列順（順A固定）**：「コード別GT集計」（転置表）・「回答別コーディング結果」ともに、コード列の並びは`codes`の元の順ではなく`codes_sorted_a`（カテゴリ出現率順→コード出現率順＝順A）に固定する。`cat_rank = {cid: i for i, cid in enumerate(cat_order)}`と`gt_count_map`（コードごとの出現件数）から`sorted(codes, key=lambda c: (cat_rank[...], -gt_count_map[...]))`で並べ替える。2026-08-03、ユーザーからの依頼で、それまでコードブック登録順（任意順）だった列順を順Aに統一した。
- **「カテゴリー出現率順位」行（新設）**：「コード別GT集計」の最上行（「カテゴリ出現数」行の上）に追加。値は各コードが属するカテゴリの出現率順位（`cat_rank[cat_id] + 1`、1位＝出現件数合計が最大のカテゴリ）で、同じカテゴリに属するコードは全て同じ順位数値になる。行の追加に伴い、`CAT_ID_ROW_INDEX`（「中間カテゴリID」行）・`PCT_ROW_INDEX`（「出現率(%)」行、20%以上/2%以下の強調表示に使う）は`gt_labels.index(...)`で動的に求める（行の増減があってもズレない）。

#### 5.5.2 回答分類・リスクチェックの反映（特記情報集計・フラグ列）

- **特記情報集計**：両シートとも見出しを「■ センチメント集計」から「■ 特記情報集計」に変更し、`info_items`（ws側で1度だけ構築し、ws2でも再利用する共通リスト）の順にセンチメント3種＋非該当＋回答分類（不明・無回答、常時）＋有効化されたリスクチェック項目（`enabled_risks`で絞り込んだ`RISK_CHECK_OPTIONS`の順）を件数・割合の2列セットで並べる。未有効化のリスク項目は列自体が生成されない（回答分類の2項目は常に生成される）。
- **回答別コーディング結果のフラグ列**：`FLAG_COLUMNS`＝「非該当」＋回答分類2項目（不明・無回答、常時）＋有効化されたリスク項目ぶんの一文字見出し列（`非`固定＋`ANSWER_TYPE_OPTIONS`の`char`＋有効な項目の`char`。例：クレームと危険情報のみ有効なら「非、不、無、ク、危」の5列）を、列1から並べる（5.5節「列配置の全体設計」参照）。各回答について、非該当は`codes`が空かどうかで判定し、それ以外は`result[key]`（`unclear`はLLM判定、`unanswered`は`_code_one_batch`が回答テキストの空欄チェックで機械的に判定、リスク項目はLLM判定）が真なら`1`、そうでなければ空文字を記入する（該当セルは`FLAG_FILL`で強調）。
- この列追加に伴い、`FIXED_N`（フラグ列数＋センチメント・回答ID・回答テキストの3列）と`CODE_START`（コード列の開始位置）は`enabled_risks`の件数に応じて可変になる。**特記情報集計・コード別GT集計（転置表）も同じ`CODE_START`/`FIXED_N`を使うため、3つのブロックのコード列は常に同じ列で揃う**（有効化リスク数が変わると開始位置がずれる点に注意）。
- **オートフィルター**：回答別コーディング結果の見出し行から最終回答行・最終コード列までの範囲に`ws.auto_filter.ref`を設定する。

#### 5.5.3 集計ダイジェストの追加要素

- **キーワード列**：コード別GT集計の最右列（8列目）に、各コードの`keywords`（セミコロン区切り文字列。4.4節の既知のギャップにより空の場合もある）を追加。
- **棒グラフ**：GT集計表の下に、`openpyxl.chart.BarChart`で画面の縦棒グラフと同じ配色・同じ並び順（順A：カテゴリ出現率順→コード出現率順）の棒グラフを追加する。集計行が1件もない場合はグラフを生成しない。
  - Excelのネイティブな棒グラフには「1系列内で棒ごとに個別の色・凡例エントリを持たせる」機能がない（単一系列に対して`DataPoint`ごとに色を付けると、Excelの「ポイントごとに色を変える」仕様により凡例がコードの数だけ増えてしまい、カテゴリ単位の凡例にならない）。この制約を回避するため、可視の集計表とは別に、J列以降へ非表示のヘルパー表を作成する：コード名列＋カテゴリごとに1列（`cat_order`の順）。各コード行は自分の属するカテゴリ列にのみ出現率(%)を記入し、他のカテゴリ列は空欄にする。行順は「順A」固定（可視のGT集計表の順序＝順B寄りとは独立）。
  - グラフはカテゴリ数ぶんの系列（1系列＝1カテゴリ、系列名はカテゴリ名、色は`cat_color_full`固定）として構築し、`chart.overlap = 100`で完全に重ねることで、見た目はコードごとに1本の棒・凡例はカテゴリ単位（K個）になる。
  - データ元列（J列以降）を非表示にしているため、`chart.visible_cells_only = False`（XML上は`plotVisOnly`）を明示的に設定する。openpyxlの既定値は`True`＝「表示セルのみプロット」であり、これを外し忘れるとExcelは非表示列のデータを一切プロットせず、タイトル・凡例枠だけが残ってグラフ本体（棒）が完全に空になる。
  - X軸のカテゴリ（コード名）ラベルは、openpyxlの`chart.set_categories()`を使わず、`series.cat = AxDataSource(strRef=StrRef(f=...))`を各系列に直接設定する。`set_categories()`は参照先の実際の値の型（文字列／数値）に関わらず常に`NumRef`として埋め込むため、コード名のような文字列を渡すとExcel側でX軸に項目名が一切表示されない（グラフの棒自体は出るが、軸ラベルが空白になる）。
- **グラフ下の集計表**：グラフの下（`chart_anchor_row + 23`行目、グラフの高さ11cm＝既定行高で約21行ぶんに余白を足した位置）に、「コード名」「コード出現率(%)」「コード出現数」の3行×コード数ぶんの列からなる簡易集計表を追加する。列順はグラフと同じ`gt_sorted_a`（順A）を再利用し、各コード列の背景は`cat_color_full`（グラフの棒と同じ色）で塗る。2026-08-03、ユーザーからの依頼でグラフ本体はそのままに、コードごとの数値を読み取りやすい表として追加した。

これらは2026-08-01に実装・検証済み（`create_excel`単体をダミーデータで呼び出し、色・列位置・フラグ値・オートフィルター・キーワード列・グラフ生成を確認）。2026-08-03、当初のDataPoint個別着色方式がExcel上で凡例崩れ（コードごとに凡例が増える）・並び順不整合（意図した順Aになっていない）を起こす不具合が判明したため、上記の非表示ヘルパー表＋複数系列方式に置き換え、標準スクリプトで再検証済み（系列数・系列色・系列順・ヘルパー表の非表示設定・値の空欄化を確認）。ただしこの置き換え直後の版は`visible_cells_only`未設定のままだったため、実際にユーザーが生成したレポート（2026-08-03 05:14生成分）でグラフが完全に空（タイトル・凡例のみで棒が一切表示されない）になる不具合が発生。原因はExcelの既定「表示セルのみプロット」設定により非表示列（J列以降）のデータが除外されたことで、`chart.visible_cells_only = False`を追加して解消・再検証済み。さらに同日、この版を実際に生成・確認したユーザーから「グラフにコード名の項目（X軸ラベル）が付いていない」「Excelのグラフ・集計レポートの色分けを画面（WEBアプリ）の配色に揃えてほしい」という指摘を受け、上記のX軸ラベル未表示バグ（`set_categories()`のNumRef固定問題）を`StrRef`直接指定で修正し、`CHART_COLORS`の値をPlotly標準配色からStreamlitテーマの実際の配色に差し替えて再検証済み。

#### 5.5.4 自由回答一覧シート

2026-08-05追加。コードブック／回答別コーディング結果に次ぐ3つ目の主要アウトプットとして、カテゴリー・コードごとに実際の回答文を並べた一覧表を追加した（ユーザー提供のRAWデータ`TEST用データ\対話がない理由_2_3_338.xlsx`をきっかけに設計。4.0節の`items`データモデル拡張＝チャンクAが前提）。

- **行の単位**：1回答が複数コードを持つ場合、コードの数だけ行を複製する（「(回答, コード)」の組み合わせが1行）。列はA列（見出し専用、後述）に続けて「カテゴリー名」「コード名」「回答者ID」「FA番号」「自由記述（該当箇所）」＋属性列（`items`の`attrs`キーを最初に登場した順に列挙。列数・列名は可変）を`DATA_START`（B列）から並べる。
- **「自由記述（該当箇所）」列は回答全文ではなく該当文の抜粋（2026-08-16変更）**：`_excerpt_for_code(res, cid, fallback_text)`が、5.1.1節の文単位データ（`res['sentences']`）からそのコード`cid`が付与された文だけを抜き出し、`／`で連結して返す（該当文が複数ある場合も全て連結する）。文単位データが無い場合（形式が古い結果など）は回答全文にフォールバックする。以前は1回答が複数コードに該当すると、その回答の全文がコードの数だけそのまま繰り返し引用され、長い回答ほど読みにくく・同じ文面が何度も出てくるという問題があった。原因（回答単位でしか根拠を持っていなかったこと）を、5.1.1節の文単位コード判定の導入により解消した。
  - **該当文が複数ある場合を1文に絞る案は不採用（2026-08-16検討）**：同一コードが1回答の複数文にまたがって付与されるケース（教員不足系のコードで句点区切りの4文すべてに同一コードが付与された実例で発覚）で、いったんは「先頭1文＋（他N文）」と表示を絞る修正を試したが、ユーザーの判断により差し戻した。理由：複数文にまたがるケースは、経緯や状況の流れそのものが該当の根拠になっていることが多く、冗長であっても全文を連結して見せた方が、1文だけに絞るより状況が分かりやすいため。
- **並び順**：`codes_sorted_a`（5.5.1節で既に構築済みの順A＝カテゴリ出現率順→コード出現率順）から`code_rank`（コードIDごとの順位）を作り、これを主キーにソートする。**副キーとして属性値（`attrs`の値を、列として使われている順＝属性１→属性２→属性３…の順）** を追加し、同じコード内では属性の値順（文字列としての昇順）に並べる。同じカテゴリ・同じコードの行は`code_rank`が同一のため自然に連続する。2026-08-05、ユーザーからの依頼で属性順ソートを追加（それまでは同じコード内では元の回答順のままだった）。
- **見出し・小見出し行（A列専用マーカー方式）**：カテゴリーが切り替わる行の直前に「■ カテゴリー：〜」（14pt）、コードが切り替わる行の直前に「▶ コード：〜」（12pt）を挿入する。**テキストはA列（`MARK_COL`）1セルのみに入れる**が、**背景色は行の区切りとして視認しやすいよう最終列（`last_col = DATA_START + len(headers) - 1`）まで塗る**（セル結合はしない。B列以降は値を持たないが同じ色で塗りつぶす）。カテゴリー見出しは`cat_color_full`、コード見出しは`cat_color_pale`で塗る。2026-08-05、当初は全列を結合した1行バナーだったが、ユーザーの依頼で「カテゴリー変わり目の行に1つ、コード変わり目の行に1つ」を別々のセルに分けて入れる方式（＝専用の見出し列）に変更。直後にユーザーから「見出し行の背景色がA列までしか指定されていない」と指摘があり、テキストはA列のみのまま、背景色だけ最終列まで塗るよう修正した。さらにその後、表タイトル（A1）を16pt、カテゴリー見出しを14pt、コード見出しを12ptに統一し、B4以降の列見出し行（カテゴリー名・コード名…）は`wrap_text=False`で自動折り返しをオフにした。
- **非該当（コードが1つも付かなかった回答）**：一覧から除外せず、全カテゴリーの後に「■ 非該当（コードなし）」セクションとしてまとめて表示する（カテゴリー名・コード名は空欄）。このセクション内も属性値順にソートする。
- **属性の扱い**：`items`の`attrs`はコーディング判断（LLMへの問い合わせ）には一切使われず、このシートの表示・ソートのためだけに保持されている値をそのまま使う（4.0節参照）。
- オートフィルターは設定しない（見出し・小見出し行が混在する一覧形式のため、フラットな表向きの機能とは相性が悪い）。`freeze_panes`はB列・見出し行の直下（データ開始位置）に設定。

2026-08-05、`create_excel`単体を合成データ（複数コード付与・非該当・属性値の順不同を含む）で呼び出し、見出し・小見出しの生成順序、複数コード行の複製、属性順ソート（元の回答順とは異なる順になることを確認）、非該当セクションの位置、属性値の反映を確認。さらに実際のテストデータ（338件）を疑似コードブックで機械的に割り当てて流し込み、実データ規模でも例外なく生成でき、データ行数が一致することを確認済み。

#### 5.5.5 文単位データシート（2026-08-16追加）

コードブック／回答別コーディング結果／自由回答一覧に次ぐ4つ目のシート。5.1.1節の文単位コード判定で得られる生データ（`res['sentences']`）を、そのまま1行1文のフラットな表として出力する。他の集計・別分析への再利用を想定した**RAWデータ専用シート**であり、見出し・色分けなどの装飾は最小限。

- **行の単位**：1文＝1行。列は「回答ID」「FA番号」＋属性列（自由回答一覧5.5.4節と同じ`attr_keys`）＋「文番号」「文テキスト」「センチメント」「該当コードID」「該当コード名」。該当コードが複数ある文は`; `区切りで1セルにまとめる（該当なしは空欄）。
- **並び順**：`results`の順（＝コーディング時の回答順）のまま、各回答内は文番号（idx）の昇順。ソートや見出し行の挿入は行わない（自由回答一覧のようなカテゴリー・コード単位の整理は他シートの役割とし、このシートは生データの提供に徹する）。
- 属性列・回答ID・FA番号の意味は自由回答一覧（5.5.4節）と同じ。`sentences`が空の回答（無回答など）は行が生成されない。

2026-08-16、`create_excel`単体を、複数文・複数コードを含む合成データで呼び出し、シートの存在・見出し列・データ行数（回答ごとの文数の合計と一致）・回答ID/コードIDの対応を標準スクリプトで確認済み。

---

## 6. 既知の制約・注意点（まとめ）

- **コード数上限による打ち切り**：上限に達すると差分検出ループが即座に終了し、以降のデータは新規コード検出の対象から外れる（4.1参照、全モード共通）。
- **キーワードの生成が不完全**：方式C1〜C3のStage1初期コードのみ（4.4参照）。
- **データクリーニングは最小限**：空行除去とトリムのみ。重複除去・文字コード自動判定なし（3節参照）。
- **認証は簡易実装**：`st.secrets`ベースの単純なユーザーID/パスワード照合（2節参照）。本番運用の前提ではない（パスワードリセット・多要素認証などは無い）。
- **履歴は非永続**：直近10件のみ、セッション終了で消える。
- **コスト概算は固定レート**：1USD=150円で固定（`llm_client.calc_cost_jpy`）。為替変動には追従しない。
- **プロバイダはAnthropicのみ運用**：`llm_client.py` はOpenAIにも対応する設計だが、`app.py` からは常に `'Anthropic'` を指定しており、実運用上はAnthropic経由のみ。
- **プロンプトキャッシュの閾値未満は無効化**：コードブックが小さいとキャッシュが効かないことがある（5.1参照）。エラーにはならないため、画面上でキャッシュ読込トークンが0のままでも異常ではない。

---

## 7. 裏側の共通基盤（`llm_client.py`）

- `call_llm()`：プロバイダ非依存の構造化出力呼び出し。tool_use（Anthropic）／function calling（OpenAI）を第一手段とし、失敗時はテキストからのJSON抽出にフォールバック。
- **リトライ**：最大2回。1回目は再現性のため `temperature=0` 固定、2回目は `temperature=0.5` に上げて異なる応答を試みる（呼び出し回数の多い方式Cでのコスト増を抑えるため2回までに制限）。
- **max_tokens**：8192固定。出力がこの上限で打ち切られた場合は明示的なエラー（`stop_reason=='max_tokens'`）として検出し、詳細メッセージを返す。
- **空/無効な応答の診断**：tool_use・テキストのいずれからも有効な結果が得られない場合、`stop_reason`・出力トークン数・実際の応答内容を含めた例外を送出し、呼び出し元（`get_last_error()`）経由でUIに表示する。
- **プロンプトキャッシュ**：`call_llm(..., system=..., cache_system=True)` を指定すると、Anthropic呼び出し時に`system`全体へ`cache_control: {"type": "ephemeral"}`を付与する（5.1節の`llm_code_batch`で使用）。`system`未指定時・`cache_system=False`時は従来通り単純なsystemパラメータ渡し、またはsystemなしになる。OpenAI側は`system`をsystemロールメッセージとして渡すのみ（自動キャッシュのため明示指定は不要）。
- **コスト計算**：`calc_cost_jpy(input_tokens, output_tokens, model, cache_read_tokens=0, cache_creation_tokens=0)` はモデル名に`'haiku'`を含むかどうかで単価を切り替える（Haiku 4.5: 入力$1/出力$5、それ以外はSonnet単価: 入力$3/出力$15、per 1M token）。キャッシュ読込分は入力単価の0.1倍、キャッシュ書込分は1.25倍（5分TTL）で加算し、1USD=150円で円換算する。
- **トークン使用量の累積**：`_token_usage`（`get_token_usage()`で取得）は`input`/`output`/`cache_read`/`cache_creation`に加え、呼び出しごとに`calc_cost_jpy()`で計算した`cost_jpy`を累積で保持する。コードブック策定（Sonnet）とコーディング（Haiku）でモデルが混在する1回の分析でも、呼び出し単位で正しい単価が適用され、合計コストが正確になる（画面側は単一モデルで再計算するのではなく、この`cost_jpy`をそのまま表示する）。

---

## 8. コードブック策定方式の比較データ（参考）

984件データでの実測比較（2026-07-29時点、「比較テスト結果.xlsx」より）：

| 作業 | 入力トークン | 出力トークン | 円換算 |
|---|---|---|---|
| 方式Cによる総合分析（策定＋コーディング） | 371,009 | 49,376 | ¥278 |
| 上記で作成したコードブックでコーディングのみ | 335,955 | 37,671 | ¥236 |

※ コードの再利用のみだと約15%（¥42）安い程度で、Step2（全件コーディング）がコストの大部分を占めることが分かっている。3000〜5000件規模での再検証は今後の予定（9節）。

---

## 9. 今後の計画（未実装・合意済みの方向性）

1. ~~**Codebook編集機能（チャット形式）**~~ → **実装済み（4.10節参照。※節番号の誤りを修正、以前は誤って「4.9節」と記載していた）**。ただし2026-08-19、手動編集UIはホームタブから一時撤去し、独立タブへの移設待ち（バックログ最後、詳細は4.10節）。
2. **Codebook Builder方式の絞り込み**：3000〜5000件規模の比較テスト結果を見た上で、A/B/C1/C2/C3を絞り込む。
3. ~~**コーディング精度調整機能（厳密↔柔軟）**~~ → **実装済み（4.5.2節・5.1節参照）**。標準／厳密／柔軟の3段階。
4. ~~**リスクチェック機能（サイドバー〜画面表示）**~~ → **チャンクA実装済み（4.5.3節・5.1節・5.3節・5.4節参照）**。
5. ~~**リスクチェックのExcel反映（チャンクB）**~~ → **実装済み（5.5節参照）**。特記情報集計への項目追加、カテゴリ別カラーの画面連動、フラグ列、オートフィルター、集計ダイジェストのキーワード列・棒グラフを実装。
6. 本ドキュメント（SPEC.md）は、上記を含む実装変更のたびに更新する。
